---
name: ship-entwurf
description: Liefert das Ergebnis eines abgeschlossenen Penpot-Entwurfsrundenlaufs aus — misst den Diff selbst, prüft ihn gegen eine geschlossene Pfad-Zulassungsmenge, committet pfadgenau, gleicht mit `main` ab, pusht und eröffnet einen Pull Request. Nutze diesen Skill, wenn ein Rundenlauf mit dem Anker `## Entwurfslauf abgeschlossen: Pull Request erwünscht` endet, oder wenn Daniel direkt danach fragt. Keine Perspektivenrunde, kein Copilot-Review, keine Spec-Finalisierung, kein Merge.
---

# Ship Entwurf — der Auslieferpfad eines Entwurfsrundenlaufs

**GitHub-Erlaubnisstufe:** lesend und schreibend
