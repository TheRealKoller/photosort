# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Node / Frontend
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.*
!.env.example
!.env.demo.example

# Docker / data volumes
photo-cache/

# Modell-Assets per verifiziertem Download statt Git-Commit
# (specs/decisions/0033-modell-asset-download-statt-commit-label-embedder.md): ueberschreitet
# GitHubs 100-MiB-Push-Limit, wird stattdessen bei Bedarf per scripts/fetch-label-embedder-model.sh
# heruntergeladen und SHA256-verifiziert (backend/Dockerfile, .github/workflows/ci.yml,
# docs/setup.md). Nur dieses eine Asset - alle anderen Modell-Assets (*.tflite/*.hdf5/*.task,
# label_embedder_tokenizer.json) bleiben unveraendert committet, kein pauschaler assets/-Ausschluss.
backend/src/photosort/assets/label_embedder.onnx

# Optionale echte Urlaubsfotos fuer das temporaere Design-Labor
# (specs/features/0287-design-richtungen-vergleich.md). Familienfotos gehoeren NIE ins Repository
# (CLAUDE.md); zusaetzlich zu diesem Eintrag prueft frontend/design-lab/guards.test.ts den
# tatsaechlichen Git-Index, weil .gitignore gegen `git add -f`, bereits getrackte Pfade und eine
# danebenliegende Datei wirkungslos ist. Mit dem Labor wieder zu entfernen.
frontend/design-lab/photos-local/

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
