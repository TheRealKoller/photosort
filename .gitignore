# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Node / Frontend
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.*
!.env.example
!.env.demo.example

# Browsergestuetzte Oberflaechenpruefung (specs/features/0174-browser-zugang-fuer-claude.md,
# specs/decisions/0058-browsergestuetzte-oberflaechenpruefung.md): Laufzeit-Ausgaben und
# Wegwerf-Skripte des e2e-Pakets. artifacts/ enthaelt Screenshots (erstmals schreibt ein Werkzeug
# Bilddateien in den Arbeitsbaum - die Projektregel "nie Bilddaten im Repo" wird zusaetzlich per
# CI-Schritt abgesichert) sowie Traces mit Netzwerkinhalten; .auth/ enthaelt den gespeicherten
# Anmeldezustand samt 30 Tage gueltigem, nicht widerrufbarem JWT und wird nie hochgeladen.
e2e/artifacts/
e2e/scratch/
e2e/.auth/
e2e/test-results/
e2e/playwright-report/

# Ansichtsentwuerfe aus Penpot (specs/features/0358-projektverwaltung-entwurf.md,
# specs/decisions/0069-ansichtsentwuerfe-als-handarbeit-mit-soll-struktur-im-repository.md
# Abschnitt 7): Formexporte der Ansichtsbretter, die an den Pull Request gehaengt und NICHT
# eingecheckt werden. Bewusst ein eigenes Verzeichnis statt e2e/artifacts/ - das gehoert der
# browsergestuetzten Oberflaechenpruefung, und zwei Werkzeuge in einem Ausgabeverzeichnis waeren
# ab dem ersten Aufraeumen ein Raetsel. Dieser Eintrag ist die tragende Haelfte; der CI-Schritt
# "keine Bilddatei im Git-Index" ist ein Detektor nach dem Push, kein Verhinderer.
design/penpot/ansichten/

# Unverkleinerte Originale der Beispielbilder. Der eingecheckte, auf 2048 px verkleinerte Satz
# liegt unter assets/beispielbilder/ (siehe dessen README.md); die Originale bleiben lokal, damit
# 25 MB nicht dauerhaft in jedem Klon eines sonst reinen Textrepos liegen.
example-pictures/

# Docker / data volumes
photo-cache/

# Modell-Assets per verifiziertem Download statt Git-Commit
# (specs/decisions/0033-modell-asset-download-statt-commit-label-embedder.md): ueberschreitet
# GitHubs 100-MiB-Push-Limit, wird stattdessen bei Bedarf per scripts/fetch-label-embedder-model.sh
# heruntergeladen und SHA256-verifiziert (backend/Dockerfile, .github/workflows/ci.yml,
# docs/setup.md). Nur dieses eine Asset - alle anderen Modell-Assets (*.tflite/*.hdf5/*.task,
# label_embedder_tokenizer.json) bleiben unveraendert committet, kein pauschaler assets/-Ausschluss.
backend/src/photosort/assets/label_embedder.onnx

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
