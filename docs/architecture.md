# Architektur-Übersicht

**Status:** Living Document (kein Lifecycle, wird laufend aktualisiert)
**Letzte Aktualisierung:** 2026-08-24 (Korrektur der Auslieferung des Label-Embedder-ONNX-Assets, ADR [`decisions/0033-modell-asset-download-statt-commit-label-embedder.md`](../specs/decisions/0033-modell-asset-download-statt-commit-label-embedder.md): `backend/src/photosort/assets/label_embedder.onnx` (118.054.609 Bytes) überschreitet GitHubs 100-MiB-Hard-Limit für einen regulären Push (erst beim ersten tatsächlichen Push des Feature-Branches entdeckt, `GH001: Large files detected`) — die in ADR 0032 Punkt 4 vorgesehene Commit-Strategie ("kein Laufzeit-Download") ist für dieses eine Asset technisch nicht durchführbar. Punktuelle Korrektur, nur dieses eine Asset betroffen: die Datei wird nicht mehr committet, sondern per neuem Skript `scripts/fetch-label-embedder-model.sh` von derselben, bereits recherchierten HuggingFace-Quelle (`Xenova/paraphrase-multilingual-MiniLM-L12-v2`) heruntergeladen und gegen den unverändert gültigen `LABEL_EMBEDDER_ONNX_SHA256`-Hash verifiziert — aufgerufen aus `backend/Dockerfile` (vor `pip install .`) und `.github/workflows/ci.yml`s `backend`-Job (vor `pytest`, da `TestLabelEmbedderAssets`/`TestRealAssetOutputDimension` direkt gegen die Datei auf der Festplatte laufen); für ein Bare-Metal-Dev-Setup einmalig manuell, siehe [`setup.md`](./setup.md). `label_embedder_tokenizer.json` (17 MB, unter dem Limit) bleibt unverändert committet, ebenso alle anderen Modell-Assets des Projekts. Git LFS wurde erwogen und verworfen (Bandbreiten-/Storage-Kontingent für dieses öffentliche Repo kostenlos nur sehr klein, CI-Läufe würden es schnell erschöpfen — laufende Kosten ohne erkennbaren Vorteil gegenüber der kostenlosen Download-Lösung).) **Letzte Aktualisierung:** 2026-08-23 (Kurskorrektur des Architektur-Ansatzes für Spec "Remote-Klassifizierung mit Kostenschätzung" (künftige `0055`, Inbox [`0035`](../specs/inbox/0035-remote-klassifizierung-mit-kostenschaetzung.md)) — ersetzt eine am selben Tag zuvor festgelegte, nie gemergte erste Fassung vollständig, ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md) (Datei direkt überschrieben statt superseded): das Remote-Vision-Modell klassifiziert **nicht mehr** gegen ein festes 3-Werte-Enum (PEOPLE/LANDSCAPE/DETAIL), sondern liefert pro Foto **1-3 offene, frei formulierte Schlagworte** mit Konfidenz — Kategorien ergeben sich erst datengetrieben aus diesen Ergebnissen, über eine Erweiterung des bereits produktiven `criteria.py::derive_active_categories`/`derive_category_key` (ADR 0023) um einen neuen, optionalen `dynamic_keys: frozenset[str]`-Parameter: zur Laufzeit entdeckte Remote-Label (Pseudo-Kriterien-Schlüssel `f"remote:{canonical_key}"`) werden an derselben 15%-Häufigkeitsschwelle gemessen und nehmen an derselben Pro-Foto-Gewinner-Auswahl teil wie die acht registrierten lokalen Kriterien — eine einzige vereinheitlichte Kategorie-Bildung statt zweier getrennter Systeme; `rank_photos`/`PhotoCriterionScore` bleiben unberührt (Pseudo-Keys sind nicht in `DEFAULT_CRITERION_WEIGHTS`). Neue, **projektübergreifende** Tabelle `category_labels` (kanonische Label-Registry: `canonical_key`/`display_name`/`embedding`/`created_at`) plus `photo_category_detections` jetzt **1:N statt 1:1** zu `Photo` (bis zu drei Zeilen). Ähnlichkeits-Zusammenfassung nahezu gleichbedeutender Roh-Label ("Hund"/"Hunde"/"dog") über ein neues, isoliertes Modul `label_embedding.py` (analog `aesthetics.py`): lokales, gepinntes Text-Embedding-Modell `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`, int8-quantisiert, ausgeführt über `onnxruntime`+`tokenizers` (bewusst **kein PyTorch, keine TensorFlow-Hub-Alternative** — Letztere hätte trotz bereits vorhandenem `tensorflow` ein zusätzliches, versionskritisches `tensorflow_text`-Pinning-Risiko bedeutet, recherchiert durch `research-engineer`) — neue Backend-Abhängigkeiten `onnxruntime>=1.19`/`tokenizers>=0.20`, zwei neue SHA256-gepinnte Modell-Assets, kein Laufzeit-Download. Override-Endpunkt `PUT /photos/{id}/category-override` nimmt jetzt einen Body (`{"category_key": "<canonical_key>"}`, validiert gegen die für das Foto tatsächlich vorhandenen Erkennungen, weiterhin kein Freitext) statt parameterlos genau eine Remote-Kategorie zu übernehmen. Ansonsten unverändert gegenüber der ersten Fassung: gemeinsamer Consent-Schalter (`Project.cloud_landmark_detection_enabled`/`.cloud_landmark_consent_at` → `.cloud_vision_detection_enabled`/`.cloud_vision_consent_at`, Endpunkt `PUT /projects/{id}/cloud-vision-consent`), Modulstruktur `cloud_vision.py` (providerneutrale HTTP-/Parsing-Extraktion aus `landmark.py`) + `remote_classification.py`, eigener expliziter Trigger-Job `classify_categories_remote` (kein Teil von `score-criteria`, eigene Run-Tabelle `remote_category_classification_runs`, eigenes Concurrency-Setting, kein Kandidatenpool-Vorfilter ADR 0021, Skip bereits klassifizierter Fotos), eigener Kostenschätzungs-Endpunkt (`GET .../estimate`, `COST_PER_IMAGE_USD`-Konstante leicht nach oben korrigiert wegen der jetzt größeren strukturierten Antwort), sofortige Override-Wirkung über die gezielte Zwei-Partitionen-Neusortierung `worker.py::reassign_photo_category` (`rank_photos`, ADR 0021, nur für zwei Partitionen erneut aufgerufen, keine neue Ranking-Logik). Noch nicht implementiert.) Zuvor am 2026-08-23 (Spec "Mistral als wählbare Cloud-Provider-Alternative zu Anthropic für das `landmark`-Kriterium" ([`0054`](../specs/features/0054-mistral-provider-option-cloud-landmark.md)) umgesetzt, ADR [`decisions/0031-mistral-provider-option-cloud-landmark.md`](../specs/decisions/0031-mistral-provider-option-cloud-landmark.md) (superseded die Exklusivitäts-Aussage aus ADR-0025-Punkt-1 teilweise — Anthropic bleibt Default/Empfehlung): `landmark.py` bekommt einen zweiten `LandmarkClientLike`-Client, `MistralLandmarkClient` (direkter `httpx`-REST-Aufruf gegen die Mistral Chat Completions API, `Authorization: Bearer`-Auth statt `x-api-key`, flacher `image_url`-Data-URI-String statt verschachteltem OpenAI-Schema, nativer JSON-Mode über `response_format`, Modell `ministral-3b-2512` — kleinste/günstigste vision-fähige Ministral-3-Variante, vor der Implementierung gegen die offizielle Mistral-Modelldokumentation verifiziert), exakt analog `AnthropicLandmarkClient`. Das frühere `_parse_detection` wurde in eine providerneutrale `_landmark_detection_from_json` (name/confidence-Extraktion + `[0,1]`-Klemmung) und zwei providerspezifische Antwort-Extraktionsfunktionen (`_anthropic_response_to_json`/`_mistral_response_to_json`) aufgeteilt. `Settings.landmark_provider` (`Literal["anthropic", "mistral"]`, Default `"anthropic"`, env `LANDMARK_PROVIDER`) + `Settings.mistral_api_key` (env `MISTRAL_API_KEY`) neu — reine Betreiber-/Deployment-Entscheidung, kein `Project`-Feld; `build_landmark_client()` wird zur kleinen Dispatch-Factory zwischen beiden Clients, `worker.py::run_criterion_scoring` bleibt strukturell unverändert (Factory weiterhin injizierbarer Default-Parameter). Neue additive Spalte `photo_landmark_detections.provider: str` (Migration `a2b3c4d5e6f7`), atomar mit `name`/`confidence` im selben Upsert gesetzt (`worker.py::_upsert_landmark_detection`) — hält fest, welcher Provider eine Erkennung erzeugt hat, damit ein späteres Umschalten von `LANDMARK_PROVIDER` die Herkunft bereits gescorter Fotos nicht stillschweigend verliert (bereits gescorte Fotos bleiben providerunabhängig übersprungen, ADR 0025 Punkt 3, ihr `provider`-Feld bleibt deshalb unverändert stehen). Der bestehende Einwilligungsmechanismus (`Project.cloud_landmark_detection_enabled`) bleibt unverändert und providerunabhängig. Keine neue Backend-Abhängigkeit (reiner `httpx`-Aufruf, kein Mistral-SDK) — siehe Feature-Branch `feature/0054-mistral-provider-landmark`.) Zuvor am 2026-08-21 (Spec "Sehenswürdigkeit-Erkennung (Landmark) via Cloud-Vision-API" ([`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md)) umgesetzt, ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md): **erste tatsächlich produktive Cloud-Abhängigkeit im gesamten Kriterien-Scoring-Pfad** — alle sieben bisherigen Kriterien laufen rein lokal, `CriterionSource.CLOUD` war bis hierhin ein reiner, ungenutzter Registry-Wert (ADR 0021). Neues, isoliertes Modul `backend/src/photosort/landmark.py` (analog `aesthetics.py` für die einzige schwere neue Abhängigkeit — hier isoliert wegen der einzigen neuen externen Vertrauensgrenze/eines neuen Secrets): `LandmarkApiError`, `LandmarkDetection` (`name: str | None`, `confidence: float`), `LandmarkClientLike`-Protocol, `AnthropicLandmarkClient` (direkter `httpx`-REST-Aufruf gegen die Anthropic Messages API, Modell `claude-haiku-4-5`, **kein** `anthropic`-Python-SDK — Minimalismus-Prinzip ADR 0006, konsistent mit dem einzigen bestehenden externen-HTTP-Client-Vorbild `opencloud/client.py`), `build_landmark_client()`-Factory (liest `settings.anthropic_api_key`, läuft wie `build_face_detector` nie in einem automatisierten Test). `criteria.py` bekommt nur die reine, synchrone, netzwerkfreie `compute_landmark_score(detection) -> float` (kein Namen → `0.0`, sonst die vom Vision-LLM gelieferte Konfidenz, auf `[0, 1]` geklemmt) sowie den neuen Registry-Eintrag `landmark` (`display_name="Sehenswürdigkeit"`, `source=CriterionSource.CLOUD`, `category_eligible=True`, `category_presence_threshold=0.5`, neue Konstante `_LANDMARK_CATEGORY_PRESENCE_THRESHOLD`) — hält den bestehenden synchronen `criteria.py`-Vertrag aller sieben lokalen Kriterien unangetastet. `worker.py::run_criterion_scoring` bekommt eine neue, consent-gegate Landmark-Phase NACH der bestehenden (rein lokalen) Foto-Schleife, VOR `derive_active_categories`/`rank_photos`: `project.cloud_landmark_detection_enabled` (neu, additiv, Default `False`) wird EINMALIG beim Laufstart gelesen (kein Live-Reread, dokumentierte Vereinfachung) — ist der Schalter aus, wird `build_landmark_client` GAR NICHT ERST aufgerufen (kein API-Key gelesen, kein Netzwerkaufruf, Sicherheits-Muss-Kriterium der Spec, testseitig über einen bei Aufruf fehlschlagenden Builder-Stub abgesichert). Ist er an, wird die neue, reine Funktion `worker.py::_select_landmark_candidates` aufgerufen: ein Foto wird nur an die Cloud gesendet, wenn im selben Lauf `content_landscape` **oder** `gebaeude` die jeweils registrierte `category_presence_threshold` erreicht (`>=`, Wiederverwendung der bestehenden Registry-Schwellwerte, kein neuer doppelt gepflegter Grenzwert) UND noch keine `landmark`-Zeile aus einem früheren Lauf existiert (die einzige, bewusst dokumentierte Ausnahme vom sonst projektweiten "jeder Lauf scort neu"-Prinzip — ersetzt strukturell einen dedizierten Retry-Mechanismus: ein einmalig fehlgeschlagenes Foto bleibt ohne `landmark`-Zeile automatisch beim nächsten Lauf erneut Kandidat). Cloud-Aufruf ausschließlich gegen die bestehende `display`-Cache-Variante (2048×2048, nie das Original), kein GPS-/EXIF-Zugriff. Begrenzte Nebenläufigkeit über das neue, env-überschreibbare Settings-Feld `settings.landmark_api_concurrency` (`Field(default=2, ge=1)`, `LANDMARK_API_CONCURRENCY`, bewusst konservativer als `scan_download_concurrency` — reales Geld pro Anfrage, fremdes Rate-Limit) im bekannten Block+`asyncio.gather(..., return_exceptions=True)`-Muster (ADR 0020, zweite Anwendung nach dem Scan) inkl. derselben expliziten `CancelledError`-Prüfung/erneutem `raise` (`gather` reicht ein `CancelledError` einer Kind-Coroutine sonst als gewöhnliches Ergebniselement durch). Timeout 60s (Modul-Konstante `landmark.py::LANDMARK_REQUEST_TIMEOUT_SECONDS`, kein Settings-Feld — reiner technischer Wert). Ein einzelner fehlgeschlagener Cloud-Aufruf (Timeout, 4xx/5xx, fehlender Cache-Eintrag) lässt den Lauf mit `status=success` abschließen, ohne `landmark`-Zeile für dieses Foto, alle anderen Kriterien dieses Fotos bleiben unberührt (best-effort, kein Laufabbruch). Neue additive `Project`-Felder `cloud_landmark_detection_enabled: bool`/`cloud_landmark_consent_at: datetime | None` sowie neue Tabelle `photo_landmark_detections` (1:1 zu `Photo`, `photo_id` als Primary Key wie `PhotoScore`, nur angelegt wenn tatsächlich ein Name identifiziert wurde) — siehe "Datenmodell" unten. Neuer Endpunkt `PUT /projects/{id}/cloud-landmark-consent` (`api/projects.py`, Body `{"enabled": bool}`, `PUT` statt `POST` da ein Zustand gesetzt statt ein Job ausgelöst wird, Vorbild `PUT /photos/{id}/rating`) setzt beide Felder synchron (Zeitstempel bei Aktivierung, `NULL` bei Deaktivierung, kein Sonderfall bei wiederholter Aktivierung) — hängt am bestehenden router-weiten Auth-Torwächter, beide Felder zusätzlich auf `ProjectOut` exponiert. Neues Settings-Feld `Settings.anthropic_api_key: str = ""` (der Platzhalter existierte bereits in `.env.example`, war aber nie in `Settings` verdrahtet — Kommentar dort von einem nie umgesetzten früheren "Phase B"-Cloud-Entwurf korrigiert). Frontend: `frontend/src/utils/categoryLabels.ts` bekommt `landmark: 'Sehenswürdigkeit'` (Sonderzeichen-Mapping wie `gebaeude`/`Gebäude`) — sobald `landmark` die 15%-Häufigkeitsschwelle (ADR 0023) erreicht, erscheint "Sehenswürdigkeit" automatisch als Kategorie in `/projects/:id/curate`, ohne weiteren Code. Erste dedizierte Projekteinstellungs-Seite im Projekt: neue Route `/projects/:projectId/settings` (`ProjectSettingsPage.tsx`, erreichbar über einen neuen "Einstellungen"-Link in `ProjectPipelineLayout.tsx`s Sekundärnavigation) mit einem neuen, eigenen Toggle-Widget `components/ui/switch.tsx` (natives `<button role="switch" aria-checked>`, bewusst **kein** `@radix-ui/react-switch` als neue Abhängigkeit für ein einziges Bedienelement, Minimalismus-Prinzip) + Info-Popover (Muster `CriterionDetailsPopover.tsx`, hier bewusst OHNE dessen geräte­spezifische Hover-Auto-Close-Logik — ein einzelner Einstellungs-Schalter braucht nicht dieselbe Hover-Ergonomie wie Dutzende Grid-Kacheln, technische Detailentscheidung der Umsetzung). Kein zusätzlicher UI-Zustand während eines laufenden Cloud-Scoring-Laufs (bestehender generischer Fortschrittsindikator reicht). Siehe Feature-Branch `feature/0047-sehenswuerdigkeit-erkennung-cloud-vision-api`.) Zuvor am 2026-08-20 (Spec "Drei Kompositions-Kriterien: Symmetrie, Horizont-Neigung, Freiraum/Fluchtrichtung" ([`0048`](../specs/features/0048-kompositions-kriterien-symmetrie-horizont-freiraum.md)) umgesetzt, ADR [`decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md`](../specs/decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md): drei weitere `criterion_key`-Werte (`symmetrie`, `horizont`, `freiraum`), alle `category_eligible=False` (reine Ranking-Signale, keine neue Kuratierungs-Kategorie), in drei architektonisch unterschiedlichen Mustern umgesetzt. (1) `symmetrie` (`source=local_heuristic`): reine `classification.py`-Erweiterung (`compute_symmetry_score`) — Quadranten-Energie-Vergleich auf der bereits vorhandenen Laplace-Kantenkarte (`_laplace_edges_without_border_artifact`, bisher nur für `compute_uniform_area_fraction` genutzt), keine neue Abhängigkeit. (2) `horizont` (`source=local_heuristic`): neues, eigenständiges Modul `backend/src/photosort/horizon.py` (`compute_horizon_tilt_score`, analog zum eigenständigen `aesthetics.py`) — `cv2.Canny`+`cv2.HoughLinesP` (lokaler `cv2`-Import) ausschließlich auf der Graustufen-Variante eines bereits über Pillow dekodierten Bildes, nie auf `cv2.imread`/`cv2.imdecode` (Pillow bleibt alleinige Bild-I/O-Bibliothek); Kandidatenlinien werden auf `±HORIZON_MAX_CANDIDATE_ANGLE` gefiltert, die längste gewinnt (Tie-Break bei exakt gleicher Länge: erste in `cv2.HoughLinesP`s Rückgabe auftretende Linie, technische Detailentscheidung des `developer`-Agenten, von ADR 0026 bewusst offen gelassen); kein Kandidat gefunden → neutraler Fallback `0.5` (bewusste Abweichung von der `goldener_schnitt`-Präzedenz mit niedrigem Fallback — Abwesenheit einer geraden Struktur ist kein Hinweis auf eine schiefe Aufnahme). Neue explizite `backend/pyproject.toml`-Abhängigkeit `opencv-contrib-python` (bewusst dieselbe Distribution, die `mediapipe` bereits transitiv installiert, **nicht** `opencv-python-headless` — die vier `opencv*`-PyPI-Distributionen installieren alle ein `cv2`-Modul an denselben Ort und sind gegenseitig konfliktträchtig): **kein neuer Netto-Footprint** — `cv2` ist seit Spec 0024 bereits im Image vorhanden (mediapipes eigene transitive Abhängigkeit), keine neue Systembibliothek in `backend/Dockerfile` nötig. (3) `freiraum` (`source=local_ml`): viertes mediapipe-Task-API-Paar in `classification.py` (`FaceLandmarker` neben dem bestehenden `FaceDetector`/`ObjectDetector`/`ImageClassifier`) — `detect_face_orientation`/`FaceOrientation`/`FaceLandmarkerLike`/`build_face_landmarker`, **eigenständiger, zusätzlicher Modellaufruf, kein Ersatz** für den bestehenden, auf `content_people`/`goldener_schnitt` abgestimmten `FaceDetector`; Blickrichtung (Yaw) wird aus der von mediapipe gelieferten 4×4-Kopfpose-Rotationsmatrix (`facial_transformation_matrixes`) abgeleitet, nicht aus einem selbst konstruierten Landmark-Vektor. Neues gepinntes Modell-Asset `backend/src/photosort/assets/face_landmarker.task` (~3,6 MB, float16-quantisierte Variante ohne Blendshapes, offizielles mediapipe-Modell-Repository) mit `FACE_LANDMARKER_MODEL_SHA256`-Konstante + eigenem SHA-256-Verifikationstest, analog den drei bestehenden `.tflite`-Assets. Kein Gesicht erkannt → niedriger Fallback `0.0` (analog `goldener_schnitt`); nahezu frontaler Blick (`|yaw| < FREIRAUM_YAW_DEADZONE_DEGREES`) → neutraler Fallback `0.5`. `worker.py::_compute_content_criteria` ruft `symmetrie`/`horizont` unconditional auf (wie `content_landscape`, keine Modell-/Detektor-Abhängigkeit), `freiraum` mit eigenem Best-effort-`try`/`except` und eigenem `_try_build`-Aufruf für `face_landmarker` — alle drei Kriterien sind voneinander unabhängig, kein gemeinsamer Berechnungspfad. Keine Migration (reine Erweiterung der bereits generischen `PhotoCriterionScore`-Tabelle), kein neuer Endpunkt, keine UI-Änderung (bestehende generische `CriterionDetailsList.tsx` zeigt neue Kriterien automatisch an, Spec 0040/0041). Siehe Feature-Branch `feature/0048-kompositions-kriterien-symmetrie-horizont-freiraum`.) Zuvor am 2026-08-20 (Spec "Dateianzahl pro Unterordner im OpenCloud-Ordner-Browser" (0050) umgesetzt, ADR [`decisions/0028-ordner-browser-bilddatei-zaehlung.md`](../specs/decisions/0028-ordner-browser-bilddatei-zaehlung.md): neuer Endpunkt `GET /opencloud/folder-counts?path=<Pfad>` (`api/opencloud.py`, gleicher Router-Level-Auth-Guard wie `/opencloud/browse`) listet dieselben direkten Unterordner wie `GET /opencloud/browse` und zählt pro Unterordner parallel die rekursive Bilddatei-Anzahl, begrenzt durch `asyncio.Semaphore(settings.opencloud_folder_count_concurrency)` (neues, env-überschreibbares Settings-Feld, Default 4, analog `scan_download_concurrency` aus ADR 0020) plus `asyncio.gather(..., return_exceptions=True)` — bewusst kein Block-/Commit-Muster wie dort, da der Endpunkt nichts in die DB schreibt. Neuer, endpunkt-lokaler Helfer `_count_images_up_to_limit` konsumiert die bestehende `OpenCloudClient.walk()`-BFS-Traversierung (samt ihrem Zyklenschutz, unverändert) mit echtem Early-Exit auf Netzwerkebene: die `async for`-Schleife bricht ab, sobald die reine Anzeige-Modulkonstante `FOLDER_COUNT_LIMIT = 500` erreicht ist — `walk()` stellt danach keine weiteren PROPFIND-Anfragen mehr, keine vollständige Baum-Traversierung vor dem Kappen. Response `list[FolderCountOut]` (`path`/`count`/`at_limit`/`error: bool`, bewusst kein Freitext-Fehlerfeld) — eine einzelne fehlschlagende Unterordner-Zählung liefert nur für diesen Eintrag `error=True`, die Gesamtantwort bleibt `200`; nur ein Fehlschlag des vorgelagerten Unterordner-Listings selbst liefert weiterhin `400` wie `browse_folder`. `GET /opencloud/browse` selbst bleibt unverändert. Reiner Refactor vorab: die bisher private `worker.py::_IMAGE_EXTENSIONS` ist als öffentliche `opencloud/client.py::IMAGE_EXTENSIONS` umgezogen (`worker.py` importiert jetzt von dort) — `api/opencloud.py` darf `worker.py` (zieht `mediapipe`/`tensorflow` nach sich) nicht in den API-Request-Pfad importieren. Frontend: `api/opencloud.ts::fetchFolderCounts`, neuer Hook `useOpenCloudFolderCountsQuery` (identisches Cache-Muster wie `useOpenCloudBrowseQuery`: eigener Query-Key pro Pfad, `staleTime: Infinity`), löst eager parallel zum bestehenden Browse-Request aus; `FolderBrowser.tsx` zeigt pro Unterordner-Zeile einen von vier Zuständen (Lade-Spinner/Zahl/`"500+"`/Fehler-`"?"`), ohne Liste oder Navigation zu blockieren — wiederverwendet den bestehenden CSS-Spinner aus `components/ui/button.tsx` statt einer neuen Icon-Bibliothek. Keine Migration, keine Persistierung. Siehe Feature-Branch `feature/0050-dateianzahl-im-ordner-browser`.) Zuvor am 2026-08-19 (Architektur-Ansatz für Spec "Nur Frontend nach außen exposen: Single-Origin-API-Proxy über Frontend-nginx" (0049) festgelegt, ADR [`decisions/0027-single-origin-api-proxy-ueber-frontend-nginx.md`](../specs/decisions/0027-single-origin-api-proxy-ueber-frontend-nginx.md): Frontend-nginx (Spec 0016) bekommt einen neuen `location /api/`-Block, der API-Requests intern über das Docker-Netzwerk an `backend:8000` weiterleitet (Präfix-Strip, kein Backend-Code-Eingriff, dynamische DNS-Auflösung per `resolver 127.0.0.11` gegen bekanntes nginx+Docker-DNS-Caching) — der Backend-Host-Port entfällt danach komplett, Daniels externer Reverse Proxy braucht nur noch eine Route auf das Frontend. Noch nicht implementiert.) Zuvor am 2026-08-18 (Spec "Kategorien aus Bild-Statistiken ableiten" (0045) umgesetzt, ADR [`decisions/0023-dynamische-kategorie-ableitung-aus-kriterien-haeufigkeit.md`](../specs/decisions/0023-dynamische-kategorie-ableitung-aus-kriterien-haeufigkeit.md): `criteria.py::derive_category_key` war bisher eine fest codierte, projektunabhängige Prioritätskette (Mensch → Landschaft → Fallback "Detail") — diese Spec macht die Kategorie-Zuweisung datengetrieben. Neue, optionale `CriterionDefinition`-Felder `category_eligible: bool = False`/`category_presence_threshold: float | None = None` (Invariante `category_eligible == (category_presence_threshold is not None)`, eigener Registry-Test) machen Kategorie-Fähigkeit zu einem reinen Registry-Attribut statt einer externen Liste; genau `content_people` (Presence-Schwelle `0.5`), `content_landscape` (`0.5`), `tier`/`gebaeude` (je `0.01`, neue Konstanten) sind jetzt `category_eligible=True` — reine Qualitätskriterien (`sharpness`/`exposure`/`goldener_schnitt`/`aesthetics`) bleiben beim Default und können nie eine Kategorie bilden. Neue, reine Funktion `criteria.py::derive_active_categories(candidate_values: dict[int, dict[str, float]], threshold_fraction: float = CATEGORY_ACTIVE_THRESHOLD_FRACTION) -> frozenset[str]` (Default `CATEGORY_ACTIVE_THRESHOLD_FRACTION = 0.15`, dokumentierte, nicht kalibrierte Setzung analog `SHARPNESS_NORMALIZATION_CEILING`/`LANDSCAPE_UNIFORM_FRACTION_THRESHOLD`): ermittelt EINMAL pro `score_criteria`-Lauf, projektweit über alle Kandidaten-Fotos eines `CriterionScoringRun` (nicht pro `cluster_key`), welche `category_eligible`-Kriterien im jeweiligen Lauf tatsächlich eine eigene Kategorie bilden dürfen — ein Kriterium ist aktiv, wenn der Anteil der Kandidaten-Fotos, deren Wert die jeweilige `category_presence_threshold` erreicht/überschreitet, `>= threshold_fraction` ist (leerer Kandidatenpool → `frozenset()`, kein `ZeroDivisionError`; fehlende Werte zählen als "nicht vorhanden"). `worker.py::run_criterion_scoring` ruft die Funktion nach der bestehenden Foto-Schleife auf `candidate_values` (dort bereits vollständig im Speicher) auf und reicht das Ergebnis an die geänderte Signatur `derive_category_key(criterion_values, active_criteria)` durch, die pro Foto nur noch gegen diese aktive Menge prüft (nicht mehr gegen eine fest codierte Kette): erfüllt ein Foto mehrere aktive Kriterien, gewinnt der höchste normierte Score, Tie-Break alphabetisch nach `criterion_key`; kein Treffer → Catch-all `CATEGORY_DETAIL` (`"detail"`, unverändert); `category_key` wird generisch aus dem gewinnenden `criterion_key` gebildet (`removeprefix("content_")`, kein manuelles Mapping mehr pro Kriterium). Beabsichtigte Verhaltensänderung: `"people"`/`"landscape"`/`"detail"` sind keine bevorzugten Standardkategorien mehr, unterliegen derselben Häufigkeitsregel wie jedes andere Inhalts-Kriterium — dasselbe Foto kann je nach Lauf in unterschiedlichen Kategorien landen, auch ohne Änderung am Einzelfoto-Score. Rein kosmetisch umbenannt (keine Verhaltensänderung): `worker.py::_CONTENT_CRITERION_KEYS`/`_CONTENT_CRITERION_SOURCES` → `_IMAGE_ANALYSIS_CRITERION_KEYS`/`_IMAGE_ANALYSIS_CRITERION_SOURCES` (bezeichnen weiterhin die bildbasiert berechneten Kriterien für die Upsert-Buchhaltung, fachlich unabhängig von `category_eligible`). Keine Migration (`PhotoRanking.category_key` war bereits ein freier String), kein neuer Endpunkt. Frontend: `frontend/src/utils/categoryLabels.ts` bekommt ein explizites Anzeigenamen-Mapping nur für Sonderzeichen (`"gebaeude"` → `"Gebäude"`), `formatCategoryKey()` bleibt generischer Fallback für alle übrigen/künftigen Keys — `CategoryBadge`/`CurateCategoriesPage.tsx` sind bereits `category_key`-agnostisch (Spec 0039/0043) und benötigen keine Änderung. Siehe Feature-Branch `feature/0045-kategorien-aus-statistiken-ableiten`.) Zuvor am 2026-08-17 (Spec "Automatisierter Flow als Stepper-Übersicht mit Detailseiten je Schritt" (0042) umgesetzt, löst die in Spec 0037 getroffene UI/UX-Entscheidung "kein neuer Wizard/Stepper" bewusst ab: reine Frontend-Restrukturierung, kein Backend-Eingriff, kein neues Datenmodell, keine neue ADR (baut auf ADR [`decisions/0004-frontend-app-shell.md`](../specs/decisions/0004-frontend-app-shell.md) auf — erste Verwendung verschachtelter `react-router`-Routen + `Outlet`-Context im Projekt, reine Anwendung der bereits gewählten Bibliothek). Die bisherige `ProjectDetailPage.tsx` (fünf Sections auf einer Seite, Stand Spec 0037) ist entfernt und durch `pages/pipeline/ProjectPipelineLayout.tsx` (Route `/projects/:projectId/pipeline`, übernimmt `useProjectQuery`/Lade-/404-/Fehlerzustand/Header/Sekundärnavigation/Redirect-Guard) plus fünf eigenständige Detailseiten (`ScanStepPage.tsx`/`AusschussStepPage.tsx`/`GateStepPage.tsx`/`KriterienStepPage.tsx`/`KuratierungStepPage.tsx`, je über `useOutletContext` an `project`/`refetchProject` angebunden, kein eigener Query-Aufruf) ersetzt, ausgewählt über `pages/pipeline/PipelineStepView.tsx` (Kind-Route `:step`, feste `Record<StepId, ComponentType>`-Zuordnung). Neue, reine Ableitungslogik `utils/pipelineSteps.ts` (`computeStepStates`/`getDefaultStepId`/`getHighestReachableStepId`/`getBlockedReason`, kein Seiteneffekt) liest den vollständigen Pipeline-Fortschritt ausschließlich aus bereits vorhandenen `ProjectOut`-Feldern (`last_scan`/`last_scoring_run` inkl. `gate_confirmed_at`/`last_criterion_scoring_run`/`category_selection_enabled`) — `getDefaultStepId`/`getHighestReachableStepId` teilen sich bewusst dieselbe interne "Frontier"-Ableitung (technische Detailentscheidung, siehe Code-Kommentar dort): eine wörtliche Umsetzung von `getHighestReachableStepId` als "letzter erreichbarer Schritt unabhängig vom Erledigt-Status" hätte bei einem frischen Projekt (der `ausschuss`-Schritt ist absichtlich ungegatet, siehe Spec 0037, und bleibt erreichbar, auch bevor `scan` erledigt ist) ein anderes Ziel geliefert als `getDefaultStepId` — ein Widerspruch zur in Spec 0042 (Akzeptanzkriterium 4) explizit geforderten Konsistenz beider Funktionen. Neue Präsentationskomponente `components/Stepper.tsx` (sticky `<nav aria-label="Fortschritt der Pipeline"><ol>`, vier Zustände erledigt/aktuell/ausstehend/blockiert, Klickbarkeit ausschließlich nach `isReachable`, Blockiert-Grund über denselben Radix-Popover wiederverwendet wie `components/CriterionDetailsPopover.tsx`) sowie `components/StatusDot.tsx` (aus `ProjectDetailPage.tsx` extrahiert, jetzt vier statt zwei Konsumenten) und `hooks/useTriggerConfirmation.ts` (ebenfalls aus `ProjectDetailPage.tsx` extrahiert, unveränderte Logik aus Spec 0017 — bei mehreren getrennten Konsumenten-Dateien entfällt die bisherige "dateilokal, nur zwei Konsumenten"-Begründung). Bestehende Route `/projects/:projectId` ist jetzt ein reiner `Navigate replace`-Redirect auf `/projects/:projectId/pipeline` (Bestandsschutz für Bookmarks/Links). `PhotoGridPage.tsx`s Gate-Bestätigungs-Redirect wechselt von `/projects/:id` auf `/projects/:id/pipeline` (landet über `getDefaultStepId` automatisch beim nächsten sinnvollen Schritt). Keine Backend-/API-Änderung — alle Endpunkte/Mutationen aus Spec 0037 bleiben unverändert erreichbar. Siehe Feature-Branch `feature/0042-flow-stepper-detailseiten`.) Zuvor am 2026-08-15 (Spec "Vier zusätzliche Kriterien: Tier, Gebäude, Goldener Schnitt, Ästhetik" (0038) umgesetzt, ADR [`decisions/0022-lokale-modellwahl-tier-gebaeude-aesthetik-kriterien.md`](../specs/decisions/0022-lokale-modellwahl-tier-gebaeude-aesthetik-kriterien.md): vier weitere `criterion_key`-Werte (`tier`/`gebaeude`/`goldener_schnitt`/`aesthetics`) in `criteria.py::CRITERIA_REGISTRY`, ausschließlich lokal berechnet, kein Cloud-Aufruf. `classification.py` (bewusst erweitert, kein neues Modul, ADR 0022) bekommt zwei weitere mediapipe-Task-API-Paare neben dem bestehenden `FaceDetector`: `detect_animals`/`AnimalDetection`/`build_object_detector` (Object Detector Task API, Modell EfficientDet-Lite0, COCO-Klassen-Filter `ANIMAL_CATEGORIES`) für `tier`, `classify_scene`/`SceneLabel`/`build_scene_classifier` (Image Classifier Task API, ImageNet-1k-Modell EfficientNet-Lite0, liefert ungefiltert alle Klassifikationen oberhalb der Konfidenzschwelle) für `gebaeude` — die Allow-Listen-Filterung auf architekturbezogene ImageNet-Klassen (`ARCHITECTURE_CATEGORIES`) passiert bewusst erst in `criteria.py::compute_gebaeude_score`, nicht in `classify_scene` selbst (Testnachweis-Anforderung der Spec, dass tatsächlich gefiltert wird). `criteria.py::compute_golden_ratio_score` ist reine Geometrie ohne eigenes ML-Modell (kein neuer Bildverarbeitungsschritt) — Distanz des primären Kompositions-Subjekts (größte `FaceBoundingBox`, hilfsweise größte `AnimalDetection`, ADR 0022 Punkt 4) zu den vier Drittel-Schnittpunkten. Neues, eigenständiges Modul `aesthetics.py` (NUR für Ästhetik/NIMA, nicht in `classification.py` integriert): `compute_aesthetics`/`build_aesthetics_model` über `idealo/image-quality-assessment` (MobileNet-Backbone, Gewichte per `load_weights` statt `load_model`, siehe Security-Hinweis im Modul), lokaler `tensorflow`-Import analog zum lokalen `mediapipe`-Import, damit die schwere Abhängigkeit nicht in einen leichteren Importpfad einsickert. Neue Backend-Abhängigkeit `tensorflow>=2.16` (**nicht** `tensorflow-cpu` — liefert keine aarch64-Linux-Wheels, siehe ADR-0022-Nachtrag 2026-08-15) — einziges neues, "schweres" ML-Framework neben dem bereits vorhandenen `mediapipe` (zwei ML-Frameworks im Backend-Image statt eines). Docker-Image-Wachstum real gemessen (nicht nur geschätzt, ADR-0022-Nachtrag 2026-08-15 (2)): Baseline 1,3 GB → 4,17 GB mit diesem Feature (~+2,9 GB), spürbar über der ursprünglichen ADR-Schätzung. Drei weitere gepinnte, im Repository eingecheckte Modell-Assets (`efficientdet_lite0.tflite`, `efficientnet_lite0.tflite`, `weights_mobilenet_aesthetic_0.07.hdf5`) je mit eigenem SHA-256-Verifikationstest, analog zum bestehenden `blaze_face_short_range.tflite`-Muster. `worker.py::_compute_content_criteria` ruft `detect_person`/`detect_animals` je Foto höchstens einmal auf und verwendet das Ergebnis für mehrere abhängige Kriterien weiter (`content_people`+`goldener_schnitt` bzw. `tier`+`goldener_schnitt`), statt pro Kriterium neu zu detektieren — jedes der sechs Inhalts-Kriterien hat dabei sein eigenes, unabhängiges Best-effort-`try`/`except` (ein fehlgeschlagener Detektor lässt die übrigen, unabhängig berechenbaren Kriterien desselben Fotos unberührt). Keine neue Migration (reine Erweiterung der bereits generischen `PhotoCriterionScore`-Tabelle um vier weitere `criterion_key`-Werte). Kein neuer Endpunkt, keine UI-Änderung (rein Backend). Siehe Feature-Branch `feature/0038-vier-kriterien-tier-gebaeude-schnitt-aesthetik`.) Zuvor am 2026-08-14 (Spec "Gateführte Bewertungs-Pipeline mit Kriterien-Scoring und Kategorie-Kuratierung" (0037) umgesetzt, ADR [`decisions/0021-kriterien-datenmodell-kuratierungs-pipeline.md`](../specs/decisions/0021-kriterien-datenmodell-kuratierungs-pipeline.md): ersetzt Spec 0024 vollständig (Top-Auswahl-Mechanik), baut Spec 0003 im Kern unverändert weiter. Erste nicht-additive Migration im Projekt — `PhotoScore.category`/`.local_quality_score` und die Tabelle `top_selection_runs` sind gedroppt, `select_top_photos`/`run_top_selection`/`classification.py::classify_category`/`CategoryCandidate`/`select_top_n_with_category_mix` entfallen ersatzlos. Neue Kriterien-Registry `criteria.py::CRITERIA_REGISTRY` (freier `criterion_key: str`, kein Enum — ein neues Kriterium braucht nur einen Registry-Eintrag, keine Migration) mit Normierungsfunktionen `normalize_sharpness`/`normalize_exposure` (reine In-Memory-Transformation der bereits vorhandenen `PhotoScore`-Rohwerte, kein erneuter Bildzugriff) sowie `compute_content_people`/`compute_content_landscape` (Wiederverwendung von `classification.py::detect_person`/`compute_uniform_area_fraction`) und `derive_category_key` (datengetriebene Nachfolge der alten `classify_category`-Prioritätskette). `classification.py::detect_person` gibt seit dieser Spec `list[FaceBoundingBox]` (normierte Positionsdaten) statt `bool` zurück — Vorgriff auf die künftige Spec 0038 (Goldener Schnitt), funktional unverändert für `content_people` (`bool(detect_person(...))`). Neue, reine Funktion `ranking.py::rank_photos(candidates, weights)` (DB-frei, analog `scoring.py`/`classification.py`): gewichteter Rang-Score je Partition, renormiert bei fehlenden Kriterien statt sie mit `0` zu werten, Tie-Break niedrigere `photo_id`; die konkrete Default-Gewichtung (`worker.py::DEFAULT_CRITERION_WEIGHTS`, Gleichgewichtung aller Registry-Kriterien) ist bewusst austauschbar, keine architekturrelevante Festlegung. Neuer Worker-Job `score_criteria`/`run_criterion_scoring` (`worker.py`, ersetzt `select_top_photos`/`run_top_selection`): Guard `409`-äquivalent (`CriterionScoringGuardError`) falls die übergebene `scoring_run_id` nicht mehr dem aktuell neuesten erfolgreichen `ScoringRun` entspricht (Re-Scan/Re-Scoring während der Kuratierung); berechnet Kriterien für alle Ausschuss-Überlebenden (sharpness/exposure immer, Inhalts-Kriterien best-effort), ruft `rank_photos` je Partition (`cluster_key`×`category_key`) auf und schreibt den vollen Kandidatenpool (nicht nur Top-N) als `PhotoRanking`-Zeilen — macht "zeig die besten N" zu einer reinen Lese-Query statt eines Job-Parameters. Neues Ausschuss-Gate: additiv `ScoringRun.gate_confirmed_at: datetime | None`, vom Worker automatisch gesetzt, wenn `run_project_scoring` keine Vorschläge findet (`suggestions_found == 0`), sonst über den neuen, synchronen Endpunkt `POST /projects/{id}/confirm-ausschuss-gate` (idempotent, projektweit, kein `user_id`-Bezug). `POST /projects/{id}/score-criteria` (ersetzt `/select-top`, `api/projects.py`) nimmt keinen `top_n_per_cluster`-Parameter mehr, sondern `scoring_run_id` (aus dem neu exponierten `ScoringRunSummary.id`) — `403` bei deaktiviertem `category_selection_enabled` (dieselbe Flag-Semantik wie zuvor, nur ein neuer Endpunkt dahinter), `409` ohne erfolgreichen `ScoringRun`, `409` ohne bestätigtes Gate, `409` bei veralteter `scoring_run_id` (identischer Staleness-Guard auch im Worker-Job selbst, defense in depth). `GET /projects/{id}/photos` bekommt den neuen Query-Parameter `top_n_per_category` (`Field(ge=1, le=10)`): liefert je Partition die besten N nach `rank_position`, serverseitig gefiltert um vom aktuellen Nutzer `REJECTED`-bewertete Fotos (`user_id` aus JWT) — Backfill ist dadurch ein reiner Nebeneffekt eines erneuten Abrufs nach einer `Rating`-Änderung (SQL-`row_number()`-Fenster-Funktion NACH dem Ausschluss-Filter berechnet), kein aktiver Nachrück-Code. `SuggestionOut` verliert `category`/`local_quality_score`/den `reason="top_pick"`-Fall (Phase A setzt `suggested_status` seitdem praktisch nur noch `REJECTED`, siehe ADR 0021) — der Kuratierungs-Kontext lebt stattdessen in einem neuen, separaten `PhotoOut.ranking`-Feld (`RankingOut`: `cluster_key`/`category_key`/`rank_score`/`rank_position`), nur befüllt im `top_n_per_category`-Query-Modus (technische Umsetzungsentscheidung, von der Spec explizit an den `developer`-Agenten delegiert). Frontend: `ProjectDetailPage.tsx` bekommt drei neue Sections (Ausschuss-Gate, Kriterien-Bewertung, Kategorie-Kuratierung) statt der bisherigen "Top-Fotos auswählen"-Section; `PhotoGridPage.tsx` erweitert um einen Gate-Modus (`&gate=1`: Banner + Bestätigungs-Button, kein neuer Screen); neue, eigenständige Route `/projects/:projectId/curate` (`CurateCategoriesPage.tsx`) — gruppiert nach Kalendertag, Foto-Moment (Zeitfenster-Cluster mit sprechender Tageszeit-Bezeichnung + Uhrzeitspanne statt der technischen `cluster_key`-ID, seit [`features/0039-kuratierung-tage-und-benannte-cluster.md`](../specs/features/0039-kuratierung-tage-und-benannte-cluster.md), `utils/timeOfDay.ts`) und Kategorie, "Verwerfen" zeigt In-place einen Skeleton-Platzhalter bis das nachgerückte Foto eintrifft (reiner Query-Refetch-Effekt über einen geteilten React-Query-Key-Präfix mit der bestehenden Rating-Invalidierung, kein aktives Nachrücken im Frontend-Code); `CategoryBadge`/`utils/categoryLabels.ts` und `utils/qualityLevel.ts` generisch für freie `category_key`-Strings bzw. normierte `[0,1]`-`rank_score`-Werte umgebaut (kein festes 3-Werte-Enum/keine feste Übersetzungsliste mehr). Siehe Feature-Branch `feature/0037-gatefuehrte-bewertungs-pipeline`.) Zuvor am 2026-08-13 (Spec "Scan-Performance: Enumerationsphase, begrenzte Parallelisierung, echter Prozent-Fortschritt" (0036) umgesetzt: `worker.py::run_project_scan` in drei Schritte aufgeteilt — Phase 1 `_enumerate_scan_entries` (materialisiert `client.walk()` zu einer In-Memory-Liste, periodisch `ScanRun.files_found`/`last_progress_at` zwischen-committet, danach `total_files = len(entries)` gesetzt und `files_found` auf 0 zurückgesetzt), Phase 2a `_classify_scan_entries` (reine, Session-lose Funktion, isoliert unit-testbar: entscheidet pro Eintrag Skip [Endung/unveränderter Etag] vs. Arbeitsposten), Phase 2b `_process_scan_block` (feste Blöcke der Größe `settings.scan_download_concurrency`, Default 4, env-überschreibbar `SCAN_DOWNLOAD_CONCURRENCY`: sequentiell `Photo`-Zeilen anlegen/aktualisieren + `flush()` ohne `commit()`, danach die reinen I/O-Coroutinen `_fetch_and_thumbnail` parallel per `asyncio.gather(..., return_exceptions=True)`, ein `commit()` pro erfolgreichem Block). Verifizierter Python-Async-Fallstrick dabei gefunden und behoben: `asyncio.gather(..., return_exceptions=True)` reicht ein `CancelledError`, das eine einzelne Kind-Coroutine wirft, als gewöhnliches Ergebniselement durch statt es zu propagieren — `_process_scan_block` prüft die Ergebnisliste deshalb explizit auf `CancelledError`-Instanzen und wirft sie manuell erneut, damit der bestehende `except asyncio.CancelledError`-Vertrag aus ADR 0019 unverändert funktioniert (siehe ADR [`decisions/0020-scan-enumeration-und-parallele-verarbeitung.md`](../specs/decisions/0020-scan-enumeration-und-parallele-verarbeitung.md)). Additiv `ScanRun.total_files: int | None` (`None` = Phase 1 noch nicht abgeschlossen, unterscheidet sich explizit von `0` = leeres Projekt), durchgereicht über `ScanSummary.total_files` (`api/projects.py`). Frontend (`ProjectDetailPage.tsx`): 1:1-Wiederverwendung des bestehenden `Progress`-Fortschrittsmusters von `ScoringRun`/`TopSelectionRun`, mit Sonderfall Phase 1 (`total_files === null`): indeterminierter Balken + Text "Dateien werden gezählt…" statt der späteren "X von Y"-Form, siehe [`specs/architecture/0004-design-system.md`](../specs/architecture/0004-design-system.md). Kein neuer persistierter Walk-Fortsetzungspunkt — Resume bleibt wie bisher über den Etag-Vergleich gelöst, jetzt explizit regressionsgetestet (Abbruch mitten in einem Block wird bei einem Neustart vollständig, ohne Duplikate und ohne erneuten Download bereits committeter Blöcke nachverarbeitet). Siehe Feature-Branch `feature/0036-scan-performance-zweiphasig-parallel`.) Zuvor am 2026-08-09 (Spec "Scan-Hänger-Detektion: Fortschritts-Watchdog gegen dauerhaft hängende Job-Läufe" (0034) umgesetzt: Zwei-Schichten-Watchdog gegen dauerhaft hängende Scan-/Scoring-/Top-Selection-Läufe, siehe ADR [`decisions/0019-job-lauf-heartbeat-watchdog.md`](../specs/decisions/0019-job-lauf-heartbeat-watchdog.md). Schicht 1 (Exception-basiert): `asyncio.CancelledError` wird jetzt in `run_project_scan`/`run_project_scoring`/`run_top_selection` (`worker.py`) vor dem bestehenden `except Exception` zusätzlich abgefangen; neue Hilfsfunktion `_fail_run(session, run, error_message)` setzt den Lauf sofort auf `FAILED`, danach `raise` (kein Verschlucken einer `BaseException` — arqs eigene Task-/Retry-Buchhaltung bleibt unbeeinflusst). Schicht 2 (fortschrittsbasiert, der eigentliche Watchdog): additive Spalte `last_progress_at` auf `ScanRun`/`ScoringRun`/`TopSelectionRun` (siehe Datenmodell unten), gepflegt an den bereits bestehenden periodischen Fortschritts-Checkpoints (keine neue Kadenz); neuer periodischer `arq`-Cron-Job `reap_stalled_runs` (`worker.py`, alle 5 Minuten, `run_at_startup=True`, erste Nutzung von `arq`s Cron-Mechanismus im Projekt) setzt jede `RUNNING`-Zeile mit `last_progress_at` älter als `STALL_THRESHOLD=15min` unabhängig von der zugrunde liegenden Job-Coroutine auf `FAILED` — bewusst fortschritts- statt zeitbasiert, damit legitim lange Scans großer Fotobibliotheken durchlaufen können. `WorkerSettings.functions` registriert `scan_project`/`score_project`/`select_top_photos` jetzt über `arq.worker.func(fn, timeout=86400, max_tries=1)` statt der arq-Defaults (`timeout=300`, `max_tries=5`) — 24h nur als großzügiger Not-Anker, `max_tries=1` deaktiviert arqs automatischen Hintergrund-Retry vollständig, damit ein abgebrochener Job keine zweite Run-Zeile erzeugt. Zusätzlich Zyklenschutz (`visited: set[str]`, geprüft vor `queue.append`) in `OpenCloudClient.walk()` gegen einen hypothetischen Zyklus in der WebDAV-Verzeichnisstruktur. Watchdog terminiert bewusst nur die DB-Zeile, nicht die zugrunde liegende `arq`-Task selbst (kein `Job.abort()`, akzeptiertes Restrisiko, siehe ADR 0019). Siehe Feature-Branch `feature/0034-scan-haenger-fortschritts-watchdog`.) Zuvor am 2026-08-08 (Spec "Top-Fotos auswählen mit Kategorie-Mix (lokal)" (0024) umgesetzt: neues Modul `classification.py` (bewusst getrennt von `scoring.py`, damit die neue `mediapipe`-Abhängigkeit nicht in den leichten Phase-A-Importpfad einsickert) mit `detect_person`/`compute_uniform_area_fraction`/`classify_category`/`select_top_n_with_category_mix`; neuer Job `select_top_photos`/`run_top_selection` (`worker.py`, analog `score_project`/`run_project_scoring`); additiv `PhotoScore.category` (neues Enum `PhotoCategory`: `LANDSCAPE`/`DETAIL`/`PEOPLE`), neue Tabelle `TopSelectionRun` (`candidates_total`/`candidates_processed` für Live-Fortschritt, analog `ScoringRun`); `POST /projects/{id}/select-top` (`api/projects.py`, legt — wie `trigger_scan`/`trigger_score` — selbst keine Run-Zeile an, das erledigt der Job; `403` bei deaktiviertem Feature-Flag `settings.category_selection_enabled`, `409` ohne erfolgreichen `ScoringRun`), `ProjectOut.last_top_selection_run`/`category_selection_enabled`, `SuggestionOut.category`/`reason="top_pick"`; Frontend: eigenständige, vom (umbenannten) Phase-A-Trigger "Ausschuss aussortieren" getrennte Section "Top-Fotos auswählen" (`ProjectDetailPage.tsx`), `CategoryBadge`/`QualityMeter`-Komponenten (Grid-/Detailansicht). Das für die Gesichtserkennung nötige `.tflite`-Modell wird als gepinntes Binärasset direkt im Repository geführt (`assets/blaze_face_short_range.tflite`, Quelle+SHA-256 in `classification.py` dokumentiert) statt zur Build-Zeit heruntergeladen — kein Netzwerkzugriff/keine Prüfsummen-Verifikation zur Build-Zeit nötig. `backend/Dockerfile` installiert zusätzlich `libgl1`/`libglib2.0-0`/`libxcb1` (von opencv, einer transitiven `mediapipe`-Abhängigkeit, dynamisch verlinkt, in `python:3.12-slim` nicht enthalten). Löst den in `specs/roadmap.md` zuvor vorgemerkten "Phase B"-Platzhalter ab — anders als dort ursprünglich skizziert läuft die Klassifikation vollständig lokal (mediapipe-Gesichtserkennung + Pillow-Heuristik statt Cloud-Vision-LLM, siehe [`decisions/0015-lokale-kategorie-klassifikation.md`](../specs/decisions/0015-lokale-kategorie-klassifikation.md)), kein neues `cloud_aesthetic_score`-Feld — das Ranking nutzt den bereits vorhandenen `local_quality_score` aus Spec 0003 — siehe Feature-Branch `feature/0024-top-photo-selection-category-mix`.) Zuvor am 2026-08-07: Architektur-Ansatz für Spec 0024 festgelegt, Datenmodell verfeinert, noch nicht implementiert. Zuvor am selben Tag (Spec "Live-Fortschrittszähler beim Scannen eines Projekts" (0022) umgesetzt: `ScanRun.files_found` wird jetzt periodisch statt nur am Ende committet, `ScoringRun`-Docstring/Beschreibung entsprechend korrigiert — kein neues Datenmodell, keine Migration.) Zuvor am 2026-08-05 (Spec "Doku-Restrukturierung" (0019) umgesetzt: Datei per `git mv` aus `specs/architecture/0001-overview.md` hierher verschoben, Wegweiser-Tabellen in README/`CLAUDE.md`/`specs/README.md` entsprechend aktualisiert — Historie bleibt über `git log --follow` nachvollziehbar. ASCII-Systemkontext-Diagramm durch [`specs/diagrams/component-overview.d2`](../specs/diagrams/component-overview.d2)/`.svg` ersetzt, siehe ADR [`decisions/0013-diagram-tooling-d2.md`](../specs/decisions/0013-diagram-tooling-d2.md). Inhaltlich unverändert gegenüber dem vorherigen Stand.) Zuvor am 2026-07-28 (Spec "Automatische Auswahl der besten Fotos" (0003, Phase A) umgesetzt: `score_project`-Job, `scoring.py` (reine Heuristik-Funktionen), `PhotoScore`/`ScoringRun`-Tabellen, `POST /projects/{id}/score`, `PhotoOut.suggestion` — siehe Feature-Branch `feature/0003-automatic-best-photo-selection` und [`decisions/0006-local-scoring-datamodel.md`](../specs/decisions/0006-local-scoring-datamodel.md). Zuvor am 2026-07-27: Architektur-Ansatz fuer Spec 0003 festgelegt, Datenmodell verfeinert, noch nicht implementiert. Zuvor am selben Tag: Spec "Manuelle Kategorisierung" (0002) umgesetzt: `Rating`-Modell, Foto-Listing/-Filter/-Pagination, Bild-Streaming-Endpunkt, Thumbnail-/Display-Erzeugung im Worker, Grid-/Einzelbild-/Vergleichsansicht im Frontend — siehe Feature-Branch `feature/0002-manual-categorization`. Zuvor am 2026-07-21: Spec "Auth-Implementierung" (0006) umgesetzt: `POST /auth/login`, `get_current_user`-Dependency, `User`-Modell, Seed-Migration, Frontend-Login-Flow — siehe Feature-Branch `feature/0006-auth`. Zuvor am 2026-07-20: Auth-Ansatz — JWT via Bearer-Header, Argon2/PyJWT — gemäß [`decisions/0005-auth-implementation.md`](../specs/decisions/0005-auth-implementation.md) entschieden; Frontend-Grundgerüst — Routing/Server-State — gemäß [`decisions/0004-frontend-app-shell.md`](../specs/decisions/0004-frontend-app-shell.md))

## Systemkontext

PhotoSort verwaltet keine eigenen Bilddateien dauerhaft — die Fotos bleiben auf einer externen **OpenCloud**-Instanz. PhotoSort speichert Metadaten, Bewertungen und einen lokalen Verarbeitungs-Cache (Thumbnails).

![Systemkontext: OpenCloud, Backend, Postgres, Redis, Worker, Frontend](../specs/diagrams/component-overview.svg)

<sub>Diagramm-Quelle: [`specs/diagrams/component-overview.d2`](../specs/diagrams/component-overview.d2), gerendert per `scripts/render-diagrams.sh` (siehe ADR [`decisions/0013-diagram-tooling-d2.md`](../specs/decisions/0013-diagram-tooling-d2.md)).</sub>

## Komponenten

- **Frontend** (`frontend/`): React + TypeScript + Vite, als PWA installierbar. Kommuniziert ausschließlich über die Backend-API (kein direkter OpenCloud-Zugriff vom Client aus, um App-Tokens nicht im Browser zu exponieren). Routing (`react-router`) und Server-State/Datenzugriff (`@tanstack/react-query` über einen schlanken Fetch-Wrapper, `api/client.ts`) gemäß [`decisions/0004-frontend-app-shell.md`](../specs/decisions/0004-frontend-app-shell.md) — mit Spec 0006 (Auth) eingeführt (Login-Route/geschützte Routen benötigten bereits Routing), Spec 0005 baut die restlichen Projekt-Routen direkt darauf auf. `api/client.ts` hängt bei vorhandenem Token automatisch `Authorization: Bearer <token>` an (Token aus `localStorage`, siehe [`decisions/0005-auth-implementation.md`](../specs/decisions/0005-auth-implementation.md)) und löst bei 401-Antworten zentral die Abmeldung/Weiterleitung zu `/login` aus.
- **Backend** (`backend/`): FastAPI. REST-API für Projekte, Fotos, Bewertungen; Auth (JWT, `Authorization: Bearer`-Header, kein Cookie); Anbindung an OpenCloud via WebDAV; stößt Hintergrund-Jobs im Worker an. **Implementiert (Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md), ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md)):** `PUT /projects/{id}/cloud-landmark-consent` (`api/projects.py`, Body `{"enabled": bool}`, hängt am bestehenden Router-Level-Auth-Guard) setzt `Project.cloud_landmark_detection_enabled`/`.cloud_landmark_consent_at` synchron (Zeitstempel bei Aktivierung, `NULL` bei Deaktivierung) — beide Felder zusätzlich auf `ProjectOut` exponiert. **Implementiert (Spec 0001):** `/opencloud/browse`, `/projects` (CRUD + Scan-Trigger), OpenCloud-Client (`opencloud/client.py`, `opencloud/webdav_xml.py`, `opencloud/exif.py`). **Implementiert (Spec 0006):** `POST /auth/login`, `get_current_user`-Dependency (Argon2/PyJWT gemäß [`decisions/0005-auth-implementation.md`](../specs/decisions/0005-auth-implementation.md)), `/projects`- und `/opencloud`-Router sind auth-pflichtig (Router-Level-Dependency). **Implementiert (Spec 0002):** `GET /projects/{id}/photos` (Listing/Filter/Pagination), `GET /photos/{id}/image` (Thumbnail-/Display-Streaming aus dem lokalen Cache), `PUT`/`DELETE /photos/{id}/rating` (`api/photos.py`, `api/ratings.py`) — auth-pflichtig per Endpoint-Dependency statt Router-Level-Dependency, da beide Router mehrere URL-Präfixe bedienen und den `User` ohnehin für die Bewertungszuordnung brauchen. **Implementiert (Spec 0003, Phase A):** `POST /projects/{id}/score` (`api/projects.py`, analog `POST /projects/{id}/scan`, gleicher Router-Level-Auth-Guard), `PhotoOut.suggestion` (`api/photos.py`, nur befüllt ohne eigenes `Rating`), `ProjectOut.last_scoring_run` (analog `last_scan`). **Implementiert (Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md), ersetzt Spec 0024):** `POST /projects/{id}/confirm-ausschuss-gate` (synchron, idempotent, projektweit), `POST /projects/{id}/score-criteria` (ersetzt `/select-top`, analog `/score`; legt selbst keine `CriterionScoringRun`-Zeile an, das erledigt der Worker-Job beim Start; `403` bei `settings.category_selection_enabled == False`, `409` ohne erfolgreichen `ScoringRun`/ohne bestätigtes Gate/bei veralteter `scoring_run_id`), `GET /projects/{id}/photos?top_n_per_category=N` (Top-N je Partition, Backfill via `REJECTED`-Filter des aktuellen Nutzers), `ProjectOut.last_criterion_scoring_run` (ersetzt `last_top_selection_run`), `ScoringRunSummary.id`/`.gate_confirmed_at` (additiv), `PhotoOut.ranking`/`RankingOut` (ersetzt `SuggestionOut.category`/`reason="top_pick"`). **Implementiert (Spec [`0050`](../specs/features/0050-dateianzahl-im-ordner-browser.md), ADR [`decisions/0028-ordner-browser-bilddatei-zaehlung.md`](../specs/decisions/0028-ordner-browser-bilddatei-zaehlung.md)):** `GET /opencloud/folder-counts?path=<Pfad>` (gleicher Router-Level-Auth-Guard wie `/opencloud/browse`) zählt parallel (`asyncio.Semaphore(settings.opencloud_folder_count_concurrency)`, Default 4) die rekursive Bilddatei-Anzahl je direktem Unterordner über einen Early-Exit-Konsum von `OpenCloudClient.walk()` (`api/opencloud.py::_count_images_up_to_limit`, Obergrenze `FOLDER_COUNT_LIMIT = 500`) — `GET /opencloud/browse` selbst unverändert. `IMAGE_EXTENSIONS` ist von `worker.py` (dort vormals privat) nach `opencloud/client.py` (öffentlich) umgezogen. **Implementiert (Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md)):** `Project.cloud_landmark_detection_enabled`/`.cloud_landmark_consent_at` sind zu `.cloud_vision_detection_enabled`/`.cloud_vision_consent_at` umbenannt (wertsicheres `ALTER TABLE ... RENAME COLUMN`, Migration `b3c4d5e6f7a8`), Endpunkt entsprechend zu `PUT /projects/{id}/cloud-vision-consent` — derselbe Schalter gated ab sofort zusätzlich die neue Remote-Kategorie-Klassifizierung, kein zweiter Consent. Vier neue Endpunkte: `GET /projects/{id}/classify-categories-remote/estimate` (`candidate_count`/`provider`/`price_per_image_usd`/`estimated_cost_usd`, funktioniert unabhängig vom Consent-Schalter), `POST /projects/{id}/classify-categories-remote` (`403` ohne Consent, `409` ohne aktuellen erfolgreichen `ScoringRun`, sonst `202`), `PUT`/`DELETE /photos/{id}/category-override` (`api/photos.py`; `PUT` nimmt jetzt `{"category_key": "<canonical_key>"}` entgegen statt parameterlos zu sein, validiert per foto-skopierter Existenzprüfung gegen `photo_category_detections`/lokal qualifizierende Kriterien dieses Fotos — `409` bei einem `category_key`, der real existiert, aber nie für dieses Foto erkannt wurde, Cross-Photo-Isolation). `PhotoOut` bekommt `remote_category_labels: list[RemoteCategoryLabelOut]` (0-3 Einträge), `category_override: str | None`, `category_candidates: list[CategoryCandidateOut]` (lokal qualifizierende Kriterien + Remote-Erkennungen dieses Fotos, technische Detailentscheidung des `developer`-Agenten zur Vermeidung von Logikduplikation im Frontend).
- **Worker** (`backend/`, eigener Container-Prozess): `arq`-basierte Jobs für Foto-Ingest (Listing, Download, Thumbnail-Erzeugung), lokale Heuristik-Berechnung und optionale Cloud-KI-Bewertung. Siehe [`decisions/0002-hybrid-ai-scoring.md`](../specs/decisions/0002-hybrid-ai-scoring.md). **Implementiert (Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md), ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md)):** `score_criteria`-Job (`worker.py::run_criterion_scoring`) bekommt eine neue, consent-gegate Cloud-Phase nach der bestehenden lokalen Foto-Schleife — erste tatsächlich produktive `CriterionSource.CLOUD`-Anbindung im Projekt (`landmark.py::AnthropicLandmarkClient`, direkter `httpx`-REST-Aufruf gegen die Anthropic Messages API, kein SDK). Vorfilterung auf `content_landscape`/`gebaeude` (Wiederverwendung der Registry-Schwellwerte), Skip bereits gescorter Fotos (einzige Ausnahme vom "jeder Lauf scort neu"-Prinzip), Block+`gather`-Nebenläufigkeit über `settings.landmark_api_concurrency` (Default 2, `LANDMARK_API_CONCURRENCY`), best-effort Fehlerbehandlung ohne Laufabbruch. Details siehe "Letzte Aktualisierung" oben und "Datenmodell" unten. **Implementiert (Spec [`0054`](../specs/features/0054-mistral-provider-option-cloud-landmark.md), ADR [`decisions/0031-mistral-provider-option-cloud-landmark.md`](../specs/decisions/0031-mistral-provider-option-cloud-landmark.md)):** `landmark.py::build_landmark_client()` ist jetzt eine kleine Dispatch-Factory zwischen `AnthropicLandmarkClient` (weiterhin Default) und dem neuen `MistralLandmarkClient` je nach `settings.landmark_provider` (env `LANDMARK_PROVIDER`) — `run_criterion_scoring` selbst bleibt unverändert, die Factory ist dort bereits als injizierbarer Default-Parameter verankert. **Implementiert (Spec [`0048`](../specs/features/0048-kompositions-kriterien-symmetrie-horizont-freiraum.md), ADR [`decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md`](../specs/decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md)):** `score_criteria`-Job berechnet drei weitere `criterion_key`-Werte (`symmetrie`, `horizont`, `freiraum`) — `symmetrie` (`classification.py::compute_symmetry_score`, keine neue Abhängigkeit) und `horizont` (neues Modul `horizon.py::compute_horizon_tilt_score`, klassische `cv2`-Kantendetektion/Hough-Transformation, `source=local_heuristic`) laufen unconditional wie `content_landscape`; `freiraum` (`classification.py::detect_face_orientation` über ein viertes mediapipe-Task-API-Paar, `FaceLandmarker`, `source=local_ml`) läuft mit eigenem Best-effort-`try`/`except` und eigenem `_try_build`-Aufruf, analog den bestehenden Detektoren. Neue Backend-Abhängigkeit `opencv-contrib-python` in `backend/pyproject.toml` — dieselbe Distribution, die `mediapipe` bereits seit Spec 0024 transitiv installiert, dadurch **kein neuer Netto-Footprint** im Docker-Image. **Implementiert (Spec [`0038`](../specs/features/0038-vier-zusaetzliche-kriterien-tier-gebaeude-schnitt-aesthetik.md), ADR [`decisions/0022-lokale-modellwahl-tier-gebaeude-aesthetik-kriterien.md`](../specs/decisions/0022-lokale-modellwahl-tier-gebaeude-aesthetik-kriterien.md)):** `score_criteria`-Job berechnet vier weitere `criterion_key`-Werte (`tier`/`gebaeude`/`goldener_schnitt`/`aesthetics`) über `classification.py` (erweitert um `detect_animals`/`classify_scene`, zwei weitere mediapipe-Task-APIs neben `FaceDetector`) und das neue, eigenständige Modul `aesthetics.py` (NIMA/MobileNet über `tensorflow`). `worker.py::_compute_content_criteria` ruft `detect_person`/`detect_animals` je Foto höchstens einmal auf und teilt sich das Ergebnis zwischen mehreren abhängigen Kriterien, statt pro Kriterium neu zu detektieren. **Implementiert (Spec [`0036`](../specs/features/0036-scan-performance-zweiphasig-parallel.md)):** `scan_project`/`run_project_scan` läuft jetzt zweiphasig — eine reine Enumerationsphase (`_enumerate_scan_entries`, kein Photo-DB-Zugriff) gefolgt von einer begrenzt parallelen Verarbeitungsphase in festen Blöcken (`_process_scan_block`, Blockgröße = neue Einstellung `settings.scan_download_concurrency`, Default 4, env-überschreibbar `SCAN_DOWNLOAD_CONCURRENCY`) — erste Intra-Job-Nebenläufigkeit im Projekt (`asyncio.gather`, bewusst statt `asyncio.TaskGroup`, siehe ADR [`decisions/0020-scan-enumeration-und-parallele-verarbeitung.md`](../specs/decisions/0020-scan-enumeration-und-parallele-verarbeitung.md)). **Implementiert (Spec [`0034`](../specs/features/0034-scan-haenger-fortschritts-watchdog.md)):** `scan_project`/`score_project`/`select_top_photos` sind über `arq.worker.func(fn, timeout=86400, max_tries=1)` registriert (24h-Not-Anker statt arq-Default 300s, kein automatischer Hintergrund-Retry); neuer periodischer `arq`-Cron-Job `reap_stalled_runs` (`worker.py::WorkerSettings.cron_jobs`, alle 5 Minuten, `run_at_startup=True`) markiert dauerhaft hängende `RUNNING`-Läufe als `FAILED` — erste Nutzung von `arq`s Cron-Mechanismus im Projekt, siehe ADR [`decisions/0019-job-lauf-heartbeat-watchdog.md`](../specs/decisions/0019-job-lauf-heartbeat-watchdog.md) und "Datenmodell" unten (`last_progress_at`). **Implementiert (Spec 0001):** `scan_project`-Job (`worker.py`) für Foto-Ingest. **Implementiert (Spec 0002):** Thumbnail-/Display-Erzeugung (`thumbnails.py`, Pillow) beim Scan, Ablage im lokalen Cache (Docker-Volume `photo_cache`), Dateiname deterministisch aus `photo_id`+`etag` (kein neues DB-Feld). **Implementiert (Spec 0003, Phase A):** `score_project`-Job (`worker.py::run_project_scoring`, analog zu `scan_project`), reine Heuristik-Funktionen in `scoring.py` (analog zu `thumbnails.py`: Schärfe/Belichtung/dHash+Hamming-Distanz, Duplikat-/Zeitfenster-Clustering), operiert auf der bereits vom Scan gecachten `display`-Variante — kein erneuter OpenCloud-Download für Phase A. Details siehe [`decisions/0006-local-scoring-datamodel.md`](../specs/decisions/0006-local-scoring-datamodel.md). **Implementiert (Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md), ersetzt Spec 0024):** `score_criteria`-Job (`worker.py::run_criterion_scoring`, ersetzt `select_top_photos`/`run_top_selection`) mit der Kriterien-Registry `criteria.py` (bewusst getrennt von `scoring.py` — die `mediapipe`-Abhängigkeit soll nicht in den Phase-A-Importpfad einsickern, wie schon zu Spec-0024-Zeiten) und der reinen Rangfolgen-Funktion `ranking.py::rank_photos`. `classification.py`s mediapipe Face Detector Task-API (gepinntes `.tflite`-Binärasset) und Pillow-Laplace-Kachel-Heuristik werden weiterhin genutzt, jetzt über `criteria.py::compute_content_people`/`compute_content_landscape`. Läuft auf ALLEN Ausschuss-Überlebenden (nicht mehr nur einem vorgefilterten Kandidatenpool pro Cluster — der bisherige Vorfilter entfällt bewusst, da `N` beim Scoren nicht mehr bekannt ist, bekannter/akzeptierter Performance-Trade-off). Details siehe [`decisions/0015-lokale-kategorie-klassifikation.md`](../specs/decisions/0015-lokale-kategorie-klassifikation.md), [`decisions/0021-kriterien-datenmodell-kuratierungs-pipeline.md`](../specs/decisions/0021-kriterien-datenmodell-kuratierungs-pipeline.md). **Implementiert (Spec [`0045`](../specs/features/0045-kategorien-aus-statistiken-ableiten.md), ADR [`decisions/0023-dynamische-kategorie-ableitung-aus-kriterien-haeufigkeit.md`](../specs/decisions/0023-dynamische-kategorie-ableitung-aus-kriterien-haeufigkeit.md)):** `criteria.py::CriterionDefinition` bekommt zwei neue, optionale Felder — `category_eligible: bool = False`, `category_presence_threshold: float | None = None` (Invariante `category_eligible == (category_presence_threshold is not None)`, durch einen eigenen Registry-Test erzwungen) — Kategorie-Fähigkeit ist damit ein reines Registry-Attribut, kein externes Mapping mehr; genau `content_people`/`content_landscape`/`tier`/`gebaeude` sind `category_eligible=True`, reine Qualitätskriterien (`sharpness`/`exposure`/`goldener_schnitt`/`aesthetics`) bleiben beim Default. Neue, reine Funktion `criteria.py::derive_active_categories(candidate_values, threshold_fraction=CATEGORY_ACTIVE_THRESHOLD_FRACTION=0.15)` ermittelt EINMAL pro `score_criteria`-Lauf, projektweit über alle Kandidaten-Fotos (nicht pro `cluster_key`), welche `category_eligible`-Kriterien im jeweiligen Lauf überhaupt eine eigene Kategorie bilden dürfen (Anteil der Fotos, die die jeweilige `category_presence_threshold` erreichen, `>= threshold_fraction`) — läuft in `worker.py::run_criterion_scoring` nach der bestehenden Foto-Schleife (auf dem dort ohnehin vollständig im Speicher gehaltenen `candidate_values`), das Ergebnis wird an die geänderte `criteria.py::derive_category_key(criterion_values, active_criteria)` durchgereicht (ersetzt die bisherige fest codierte Prioritätskette Mensch→Landschaft→Fallback). Bei mehreren gleichzeitig erfüllten aktiven Kriterien gewinnt der höchste normierte Score, Tie-Break alphabetisch nach `criterion_key`; `category_key` wird generisch aus dem gewinnenden `criterion_key` gebildet (`removeprefix("content_")`), kein manuelles Mapping mehr pro Kriterium nötig. Verhaltensänderung (beabsichtigt, kein Bug): `"people"`/`"landscape"`/`"detail"` sind keine bevorzugten Standardkategorien mehr, unterliegen derselben Häufigkeitsregel wie jedes andere Inhalts-Kriterium. Keine Migration (`PhotoRanking.category_key` war bereits ein freier String). `worker.py::_CONTENT_CRITERION_KEYS`/`_CONTENT_CRITERION_SOURCES` (bildbasiert berechnete Kriterien für Upsert-Buchhaltung, fachlich unabhängig von `category_eligible`) rein kosmetisch umbenannt zu `_IMAGE_ANALYSIS_CRITERION_KEYS`/`_IMAGE_ANALYSIS_CRITERION_SOURCES`, um Verwechslung zu vermeiden. Kein neuer Endpunkt, kein Frontend-Effekt außer einem Anzeigenamen-Mapping für Sonderzeichen (`frontend/src/utils/categoryLabels.ts`: `"gebaeude"` → `"Gebäude"`). Siehe Feature-Branch `feature/0045-kategorien-aus-statistiken-ableiten`. **Implementiert (Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md)):** neuer, eigenständiger Job `classify_categories_remote`/`worker.py::run_remote_category_classification` (KEIN Teil von `score_criteria`, eigene Run-Tabelle `remote_category_classification_runs`, eigenes Concurrency-Setting `settings.remote_category_classification_concurrency`, Default 2) — Kandidatenmenge ist der komplette Ausschuss-Überlebender-Bestand OHNE Vorfilter (anders als `landmark`, ADR 0021), Skip bereits klassifizierter Fotos (mind. eine `photo_category_detections`-Zeile), best-effort ohne Retry. Neues, providerneutrales Modul `cloud_vision.py` (aus `landmark.py` extrahiert: URLs/Modell-IDs/Timeout/Response-Parsing-Helfer, von `landmark.py` UND dem neuen `remote_classification.py` genutzt) sowie `remote_classification.py` selbst (`CategoryLabelDetection`, `AnthropicCategoryClient`/`MistralCategoryClient`, `build_category_classification_client()`, `resolve_canonical_label` — löst ein Roh-Label über einen exakten NFKC+casefold-Fast-Path oder eine Kosinus-Ähnlichkeits-Prüfung gegen die `category_labels`-Registry auf einen kanonischen Eintrag auf). Neues, isoliertes Modul `label_embedding.py` (analog `aesthetics.py`): lokales, gepinntes Text-Embedding-Modell über `onnxruntime`+`tokenizers` (neue Backend-Abhängigkeiten, CPU-only), SHA256-gepinnte Assets (kein Laufzeit-Download). `worker.py::run_criterion_scoring` merged vor dem bestehenden `derive_active_categories`/`derive_category_key`-Aufruf bereits vorhandene Remote-Label-Ergebnisse als zusätzliche `f"remote:{canonical_key}"`-Pseudo-Kriterien in dieselbe `candidate_values`-Struktur (`worker.py::_merge_remote_category_labels`, kein neuer Cloud-Aufruf an dieser Stelle) — `criteria.py::derive_active_categories`/`derive_category_key` bekommen dafür einen neuen, optionalen `dynamic_keys: frozenset[str]`-Parameter (Default `frozenset()`, bestehende Aufrufer bleiben unverändert). Neue Funktion `worker.py::reassign_photo_category` verschiebt ein Foto bei einem manuellen Override sofort im selben API-Request zwischen zwei `(cluster_key, category_key)`-Partitionen (`ranking.py::rank_photos` nur für diese zwei Partitionen erneut aufgerufen, kein neuer Ranking-Algorithmus, kein voller Re-Scoring-Lauf). `reap_stalled_runs` bekommt einen vierten, eigenständigen Tabellen-Block für `remote_category_classification_runs`.
- **Postgres**: Metadaten (Projekte, Fotos, Bewertungen, Nutzer), keine Bilddaten.
- **Redis**: Job-Queue für den Worker.
- **Lokaler Cache**: Docker-Volume für Thumbnails/Zwischenergebnisse, kein Ersatz für OpenCloud als Quelle der Wahrheit.

## Datenmodell (Skizze, wird pro Feature-Spec verfeinert)

- **User** *(implementiert, Spec 0006, `models.py`)*: Account (Daniel, Ehefrau), getrennt authentifiziert — `username` (frei, kein festes Enum) + `password_hash` (Argon2). Initial über eine idempotente Alembic-Seed-Migration angelegt, Passwörter aus Umgebungsvariablen. Künftiges `Rating` (Spec 0002) referenziert `User` über `user_id`.
- **Project** *(implementiert, `models.py`)*: z.B. "Costa Rica"; referenziert genau einen OpenCloud-Ordner (rekursiv inkl. Unterordner) über `opencloud_drive_id` + `opencloud_path`. **Implementiert (Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md), ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md)):** additiv `cloud_landmark_detection_enabled: bool` (Default `False`) — projektweiter Einwilligungs-Schalter für die Cloud-Sehenswürdigkeit-Erkennung, kein `user_id`-Bezug (konsistent mit dem "kein Innentäter-Modell"-Grundsatz) — und `cloud_landmark_consent_at: datetime | None` (Zeitstempel bei Aktivierung, `NULL` bei Deaktivierung, kein volles Audit-Log, analog `ScoringRun.gate_confirmed_at`). **Umbenannt (Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md)):** beide Felder heißen jetzt `cloud_vision_detection_enabled`/`cloud_vision_consent_at` (wertsicheres `RENAME COLUMN`, Migration `b3c4d5e6f7a8`) — gaten seitdem sowohl `landmark` als auch die neue Remote-Kategorie-Klassifizierung.
- **OpenCloud-Verbindung**: kein eigenes DB-Modell — eine einzige, instanzweite Verbindung, konfiguriert über `OPENCLOUD_BASE_URL`/`OPENCLOUD_USERNAME`/`OPENCLOUD_APP_TOKEN`/`OPENCLOUD_DRIVE_NAME` in `.env`. Details siehe [`features/0001-opencloud-project-connection.md`](../specs/features/0001-opencloud-project-connection.md).
- **Photo** *(implementiert, `models.py`)*: gehört zu einem Project, referenziert `relative_path`/`etag` auf OpenCloud, `taken_at`/`last_modified`, `content_length`. Nur JPEG/PNG/HEIC (MVP).
- **ScanRun** *(implementiert, `models.py`)*: ein (Re-)Scan-Lauf eines Projekts — Status (`running`/`success`/`failed`), Zähler (`files_found`, `photos_added`, `photos_updated`, `photos_removed`, `files_skipped`), `error_message` bei Fehlern. `files_found` wird seit [`features/0022-scan-live-fortschrittszaehler.md`](../specs/features/0022-scan-live-fortschrittszaehler.md) periodisch zwischen-committet (`worker.py::run_project_scan`, `SCAN_COMMIT_BATCH_SIZE`) statt nur einmal am Ende, damit die Projekt-Detailseite einen live wachsenden Zähler zeigen kann. Liefert die Zusammenfassung, die über `GET /projects/{id}` als `last_scan` ausgegeben wird. **Implementiert (Spec [`0034`](../specs/features/0034-scan-haenger-fortschritts-watchdog.md)):** additiv `last_progress_at: datetime` (`server_default=func.now()`, analog `started_at`), an denselben Stellen wie `files_found` zwischen-committet — rein interne Watchdog-Buchhaltung (`worker.py::reap_stalled_runs`), kein API-Response-Delta. **Implementiert (Spec [`0036`](../specs/features/0036-scan-performance-zweiphasig-parallel.md)):** additiv `total_files: int | None` (`None` = Enumerationsphase noch nicht abgeschlossen, unterscheidet sich explizit von `0` = leeres Projekt — überall `is not None` statt truthy geprüft) — wird nach Abschluss der neuen Enumerationsphase (`worker.py::_enumerate_scan_entries`) einmalig gesetzt; `files_found` wechselt danach die Bedeutung von "in Phase 1 gelistet" auf "in Phase 2 verarbeitet" (kein zweites Zählerfeld, siehe ADR [`decisions/0020-scan-enumeration-und-parallele-verarbeitung.md`](../specs/decisions/0020-scan-enumeration-und-parallele-verarbeitung.md)). Im API-Response als `ScanSummary.total_files` durchgereicht, macht den bisherigen reinen Rohzähler zu einem echten Prozent-Fortschritt auf der Projekt-Detailseite.
- **PhotoScore** *(implementiert, Spec 0003, `models.py`, Datenmodell festgelegt in [`decisions/0006-local-scoring-datamodel.md`](../specs/decisions/0006-local-scoring-datamodel.md))*: 1:1 zu `Photo` (`photo_id` als Primary Key). Ergebnis der lokalen Heuristiken aus Phase A: `sharpness`, `exposure`, `phash` (dHash, ohne neue Abhängigkeit direkt mit Pillow berechnet), `duplicate_of` (selbstreferenzierender FK auf `photos.id`), `cluster_key`, `suggested_status` (wiederverwendet `RatingStatus`, praktisch nur noch `REJECTED`), `computed_at`. Bewusst getrennt von `Rating` — ein automatischer Vorschlag ist nie eine `Rating`-Zeile, sondern wird der API/dem Frontend als eigenes Feld `PhotoOut.suggestion` neben `ratings` angeboten; erst eine explizite Nutzerbestätigung erzeugt eine echte `Rating`-Zeile über den bestehenden `PUT /photos/{id}/rating`-Endpunkt. **Entfernt (Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md), erste nicht-additive Migration im Projekt):** `category`/`local_quality_score` (Spec 0024) sind ersatzlos gedroppt — reiner, nie manuell editierter Ableitungszustand, dessen Nachfolge jetzt strukturell durch `PhotoCriterionScore`/`PhotoRanking` übernommen wird. **Implementiert (Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md)):** additiv `category_override: str | None` (ein beliebiger `canonical_key` statt eines von drei festen Werten) — dauerhafte manuelle Übersteuerung des sonst automatisch abgeleiteten `category_key`, `worker.py::run_criterion_scoring` verwendet `category_key = score.category_override or derive_category_key(...)` bei jeder künftigen Partitionsbildung.
- **PhotoCriterionScore** *(implementiert, Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md), `models.py`, ADR [`decisions/0021-kriterien-datenmodell-kuratierungs-pipeline.md`](../specs/decisions/0021-kriterien-datenmodell-kuratierungs-pipeline.md))*: eine Zeile pro (Foto, Kriterium) — generische Tabelle statt weiterer fixer `PhotoScore`-Spalten, damit ein neues Kriterium nie eine neue Migration erzwingt. `criterion_key: str` (freier String, kein Enum — siehe `criteria.py::CRITERIA_REGISTRY`), `value: float` (immer normiert `[0, 1]`, "höher = besser"), `source: CriterionSource` (`local_heuristic`/`local_ml`/`cloud` — `cloud` war bis Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md) ein reiner, ungenutzter Registry-Wert ohne Compute-Pfad — seither hat `landmark` einen echten `cloud`-Compute-Pfad, siehe unten), `computed_at`. `UniqueConstraint(photo_id, criterion_key)`, Upsert bei jedem Lauf, keine Historie. **Implementiert (Spec [`0038`](../specs/features/0038-vier-zusaetzliche-kriterien-tier-gebaeude-schnitt-aesthetik.md)):** vier weitere `criterion_key`-Werte (`tier`, `gebaeude`, `aesthetics` — `source=local_ml`; `goldener_schnitt` — `source=local_heuristic`) — keine neue Migration, reine Erweiterung des bereits generischen Wertebereichs. **Implementiert (Spec [`0048`](../specs/features/0048-kompositions-kriterien-symmetrie-horizont-freiraum.md), ADR [`decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md`](../specs/decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md)):** drei weitere `criterion_key`-Werte (`symmetrie`, `horizont` — `source=local_heuristic`; `freiraum` — `source=local_ml`), alle `category_eligible=False` — ebenfalls keine neue Migration, reine Erweiterung desselben generischen Wertebereichs. **Implementiert (Spec [`0045`](../specs/features/0045-kategorien-aus-statistiken-ableiten.md), ADR [`decisions/0023-dynamische-kategorie-ableitung-aus-kriterien-haeufigkeit.md`](../specs/decisions/0023-dynamische-kategorie-ableitung-aus-kriterien-haeufigkeit.md)):** keine Tabellen-/Spaltenänderung — die neuen `CriterionDefinition.category_eligible`/`.category_presence_threshold`-Felder in `criteria.py::CRITERIA_REGISTRY` sind reine In-Code-Registry-Metadaten (steuern, welche `criterion_key`-Werte als Kategorie-Quelle in Frage kommen), keine DB-Spalten dieser Tabelle. **Implementiert (Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md), ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md)):** ein weiterer `criterion_key`-Wert (`landmark`, `category_eligible=True`) — **erste tatsächlich produktiv geschriebene `source=CriterionSource.CLOUD`-Zeile** im Projekt (der Enum-Wert existierte bereits seit ADR 0021, war aber bis hierhin ungenutzt). Keine neue Migration, reine Erweiterung des bereits generischen Wertebereichs.
- **PhotoLandmarkDetection** *(implementiert, Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md), `models.py`, ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md))*: 1:1 zu `Photo` (`photo_id` als Primary Key, analog `PhotoScore`, nicht `id`+`UniqueConstraint` wie `PhotoCriterionScore` — eine optionale Detail-Zeile pro Foto, kein Mehrfach-Kriterien-Fact). `name: str`, `confidence: float` (bewusste kleine Duplikation zu `PhotoCriterionScore(criterion_key="landmark").value` — hält die Tabelle für eine spätere UI-Abfrage ohne Join selbsttragend, beide Werte stammen atomar aus derselben API-Antwort), `computed_at`. Nur angelegt, wenn tatsächlich ein Name identifiziert wurde (kein Platzhalter). Kein UI-Verweis in v1 — reine Persistenz-Vorbereitung, vermeidet einen späteren, erneut kostenpflichtigen Cloud-Durchlauf aller bereits gescorten Fotos, falls der Name doch einmal angezeigt werden soll. **Implementiert (Spec [`0054`](../specs/features/0054-mistral-provider-option-cloud-landmark.md), ADR [`decisions/0031-mistral-provider-option-cloud-landmark.md`](../specs/decisions/0031-mistral-provider-option-cloud-landmark.md)):** additiv `provider: str` (Python-seitiger Default `"anthropic"`, Migration `a2b3c4d5e6f7`) — hält fest, welcher Cloud-Provider (`"anthropic"`/`"mistral"`) die jeweilige Zeile erzeugt hat, atomar mit `name`/`confidence` im selben Upsert gesetzt.
- **ScoringRun** *(implementiert, Spec 0003, `models.py`)*: ein Lauf des Phase-A-Scoring-Jobs, analog zu `ScanRun` (nutzt bewusst denselben `ScanStatus`-Enum statt eines eigenen, identische running/success/failed-Semantik), mit `photos_total`/`photos_processed` für granularen, periodisch zwischen-committeten Live-Fortschritt inkl. bekanntem Nenner ("X von Y") — anders als bei `ScanRun.files_found`, das zwar seit Spec 0022 ebenfalls periodisch committet, aber wegen des lazy durchlaufenen OpenCloud-Ordnerbaums nie einen Nenner kennt — siehe [`decisions/0006-local-scoring-datamodel.md`](../specs/decisions/0006-local-scoring-datamodel.md). Liefert die Zusammenfassung, die über `GET /projects/{id}` als `last_scoring_run` ausgegeben wird. **Implementiert (Spec [`0034`](../specs/features/0034-scan-haenger-fortschritts-watchdog.md)):** additiv `last_progress_at: datetime`, analog `ScanRun` oben. **Implementiert (Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md)):** additiv `gate_confirmed_at: datetime | None` (Ausschuss-Gate, projektweit, kein `user_id`-Bezug) — vom Worker automatisch gesetzt, wenn `suggestions_found == 0`, sonst über `POST /projects/{id}/confirm-ausschuss-gate`.
- **CriterionScoringRun** *(implementiert, Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md), `models.py`, ADR 0021, ersetzt `TopSelectionRun`)*: ein Lauf des Kriterien-/Rangfolgen-Jobs, analog `ScoringRun` (nutzt denselben `ScanStatus`-Enum); `scoring_run_id` (FK, bindet den Lauf an den `ScoringRun`-Stand, dessen `cluster_key` er voraussetzt — Grundlage für den `409`-Staleness-Guard bei einem zwischenzeitlichen Re-Scan/Re-Scoring), `photos_total`/`photos_processed` für granularen Live-Fortschritt (kleinere Batch-Größe als `ScoringRun`, mediapipe-Inferenz pro Foto). Kein `top_n_per_cluster`/`candidates_total`/`suggestions_found` mehr (anders als `TopSelectionRun`) — `N` ist beim Scoren nicht mehr bekannt, der Job berechnet immer den vollen Rangfolge-Pool aller Ausschuss-Überlebenden. Liefert die Zusammenfassung, die über `GET /projects/{id}` als `last_criterion_scoring_run` ausgegeben wird.
- **PhotoRanking** *(implementiert, Spec [`0037`](../specs/features/0037-gatefuehrte-bewertungs-pipeline-mit-backfill.md), `models.py`, ADR 0021)*: der volle, sortierte Kandidatenpool einer Partition (`cluster_key`×`category_key`) für einen `CriterionScoringRun` — NICHT nur die Top-N. `category_key: str` (freier String wie `criterion_key`, kein `PhotoCategory`-Enum mehr), `rank_score: float`, `rank_position: int` (1-basiert je Partition). `UniqueConstraint(criterion_scoring_run_id, photo_id)`. Macht "zeig die besten N pro Kategorie" (`GET /projects/{id}/photos?top_n_per_category=N`) zu einer reinen Lese-Query statt eines Job-Parameters, und Backfill zu einem reinen Nebeneffekt eines erneuten Abrufs nach einer `Rating`-Änderung (SQL-`row_number()`-Fensterfunktion NACH dem `REJECTED`-Ausschlussfilter des aktuellen Nutzers).
- **Rating** *(implementiert, Spec 0002, `models.py`)*: Bewertung eines Photos durch einen User (`favorite` / `album_worthy` / `rejected`), pro User getrennt gespeichert; Unique-Constraint `(photo_id, user_id)`, "unbewertet" = fehlende Zeile (kein Enum-Wert). Bleibt mit Spec 0003 unverändert (keine neue Spalte, keine Migration bestehender Daten) — siehe ADR 0006.
- **CategoryLabel** *(implementiert, Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), `models.py`, ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md))*: kanonische Label-Registry für die offene Remote-Kategorie-Klassifizierung — bewusst **projektübergreifend** (kein `project_id`-Bezug: reine Vokabular-Einträge, keine Fotoinhalte, verhindert wiederholte Neuanlage identischer Label über mehrere Projekte hinweg). `canonical_key: str` (`UNIQUE`, Slug), `display_name: str` (zuerst gesehener Roh-Label-Text), `embedding: JSON` (384-dimensionaler Text-Embedding-Vektor, `label_embedding.py`), `created_at`. Kein Cascade-Ziel bei `DELETE /projects/{id}`.
- **PhotoCategoryDetection** *(implementiert, Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), `models.py`, ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md))*: **1:N zu `Photo`** (bis zu drei Zeilen pro Foto, ein Roh-Label je Zeile — anders als `PhotoLandmarkDetection`, das 1:1 bleibt). `raw_label: str` (ungekürzter Roh-Text, Audit-Spur), `confidence: float`, `provider: str`, `computed_at`, FK auf `CategoryLabel`. `UniqueConstraint(photo_id, category_label_id)` — verhindert Duplikate, falls zwei Roh-Labels desselben Fotos auf denselben `canonical_key` auflösen (höhere `confidence` gewinnt). Immer 1-3 Zeilen pro erfolgreich klassifiziertem Foto, nie 0 (anders als `landmark`, kein "nichts erkannt"-Fall).
- **RemoteCategoryClassificationRun** *(implementiert, Spec [`0055`](../specs/features/0055-remote-kategorie-klassifizierung-mit-kostenschaetzung.md), `models.py`, ADR [`decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md`](../specs/decisions/0032-remote-kategorie-klassifizierung-mit-kostenschaetzung.md))*: ein Lauf des Remote-Kategorie-Klassifizierungs-Jobs, analog `CriterionScoringRun`, aber bewusst OHNE `scoring_run_id`-FK (dieser Job schreibt ausschließlich in `photo_category_detections`/`category_labels`, berührt weder `cluster_key` noch `PhotoRanking` direkt — kein `409`-Staleness-Guard, kein Ausschuss-Gate-Erfordernis). `photos_total`/`photos_processed`/`last_progress_at` analog den übrigen Run-Tabellen. Liefert die Zusammenfassung, die über `GET /projects/{id}` als `last_remote_category_classification_run` ausgegeben wird.

## Bewusste Annahmen (können per ADR/Spec revidiert werden)

- Kein eingebauter Reverse Proxy/TLS in `docker-compose.yml` — das Homeserver-Setup von Daniel übernimmt das.
- CPU-only-Betrieb: alle lokalen KI-Heuristiken müssen ohne GPU praktikabel laufen.
- Cloud-KI-Aufrufe sind immer optional/on-demand, nie Voraussetzung für die Kernfunktion — mit Spec [`0047`](../specs/features/0047-sehenswuerdigkeit-erkennung-cloud-vision-api.md) (ADR [`decisions/0025-cloud-landmark-erkennung.md`](../specs/decisions/0025-cloud-landmark-erkennung.md)) erstmals tatsächlich eingelöst statt nur vorgesehen: das `landmark`-Kriterium ist die **erste produktive, aber projektweit standardmäßig deaktivierte** Cloud-Anbindung im Kriterien-Scoring-Pfad (Default `Project.cloud_landmark_detection_enabled=False`) — alle sieben übrigen Kriterien bleiben rein lokal, kein Projekt verlässt ohne explizite Einwilligung den Homeserver. Setzt den in ADR 0015 als vorübergehend markierten "vorerst keine remote Modelle"-Grundsatz bewusst und einmalig außer Kraft, genau für den einen Fall, für den lokale Erkennung von Anfang an als unwirtschaftlich verworfen wurde (Sehenswürdigkeit-Erkennung braucht LLM-Weltwissen, kein trainierbares lokales Modell).
- Zwei lokale ML-Frameworks im Backend-Image (Spec [`0038`](../specs/features/0038-vier-zusaetzliche-kriterien-tier-gebaeude-schnitt-aesthetik.md), ADR [`decisions/0022-lokale-modellwahl-tier-gebaeude-aesthetik-kriterien.md`](../specs/decisions/0022-lokale-modellwahl-tier-gebaeude-aesthetik-kriterien.md)): `mediapipe` (Gesicht/Tier/Gebäude/Freiraum, seit Spec 0048 vier Task-API-Paare derselben Abhängigkeit) und `tensorflow` (nur Ästhetik/NIMA, lokal importiert). Ein drittes schweres Framework (z.B. PyTorch für eine treuere Innenraum-Erkennung) wurde bewusst nicht eingeführt — Docker-Image-Wachstum ist dadurch trotzdem real gemessen erheblich (Baseline 1,3 GB → 4,17 GB, siehe ADR-0022-Nachtrag 2026-08-15), aber weiterhin kein Blocker für den Homeserver. `opencv-contrib-python` (Spec [`0048`](../specs/features/0048-kompositions-kriterien-symmetrie-horizont-freiraum.md), ADR [`decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md`](../specs/decisions/0026-modellwahl-symmetrie-horizont-freiraum-kriterien.md)) ist seit dieser Spec eine explizite, direkte `backend/pyproject.toml`-Abhängigkeit (`horizon.py::compute_horizon_tilt_score`, klassische `cv2`-Kantendetektion, kein ML-Modell) — **kein drittes schweres Framework und kein neuer Netto-Footprint**, da dieselbe Distribution bereits seit Spec 0024 transitiv über `mediapipe` im Image vorhanden war; die Zeile macht eine bisher implizite Abhängigkeit nur explizit. Die aktive `cv2`-Nutzung ist strukturell auf `cv2.Canny`/`cv2.HoughLinesP` auf bereits über Pillow dekodierten Pixeldaten begrenzt, nie auf `cv2.imread`/`cv2.imdecode` — Pillow bleibt die alleinige Bild-I/O-Bibliothek im Projekt.
