# Sicherheitskonzept

**Status:** Living Document (kein Lifecycle, wird laufend aktualisiert)
**Letzte Aktualisierung:** 2026-08-02 (Sicherheits-Konsultation zu Spec "Visuelles Redesign & UI-Komponentenbibliothek" (0012), ADR [`decisions/0011-ui-component-library.md`](../decisions/0011-ui-component-library.md), im Zuge des idea-sharpener-Ablaufs, noch nicht implementiert: geprüft, ob die neuen Frontend-Laufzeit-Abhängigkeiten (Tailwind CSS, `@radix-ui/react-*`, `class-variance-authority`, `clsx`/`tailwind-merge`, shadcn/ui-Komponenten als kopierter Quellcode in `frontend/src/components/ui/`) die in ADR 0005 als tragende Voraussetzung festgehaltene Restrisiko-Abwägung für Session-Token-in-`localStorage` (kein httpOnly-Cookie) hinfällig machen — die ADR nennt zwei Trigger-Faktoren, "nutzergenerierter HTML-Inhalt" und "Drittanbieter-Skripte". Ergebnis: **keiner der beiden Faktoren wird durch dieses Feature ausgelöst.** Radix-UI-Primitives rendern über React-Kinder/`asChild`-Slot-Klonen, nicht über `dangerouslySetInnerHTML` oder rohe HTML-String-Props, und unterlaufen die bestehende XSS-Konvention (siehe Frontend-Abschnitt unten) damit nicht von sich aus. "Drittanbieter-Skripte" im Sinne von ADR 0005 meint erkennbar Tracking-/Analytics-/Embed-Skripte mit eigener, unkontrollierter Laufzeitlogik gegen die Seite — Radix/Tailwind/shadcn sind dagegen first-party gebündelter Build-Code (kein CDN, kein dynamisches Nachladen zur Laufzeit), die eigentliche Angriffsfläche (injizierbarer fremder Content) bleibt unverändert. Der shadcn-Copy-in-Repo-Ansatz macht die neuen Basiskomponenten zusätzlich zu auditierbarem First-Party-Code statt einer Blackbox-Abhängigkeit. Keine neue Angriffsflächen-Kategorie, daher keine strukturelle Ergänzung nötig — nur die bestehende `dangerouslySetInnerHTML`-Konvention (Frontend-Abschnitt unten) wird als für die neuen/migrierten Views explizit weiterhin geltend bestätigt (Muss-Kriterium für Spec 0012, siehe dort). Zuvor am selben Tag (Sicherheitsfokussiertes Review zu Spec "Lokal ausprobieren ohne echten OpenCloud-Server" (0009), Feature-Branch `feature/0009-local-opencloud-demo-stack`: alle drei vorab benannten Muss-Kriterien geprüft und bestätigt umgesetzt — (1) `docker-compose.demo.yml` bindet den Port nachweislich per `ports: ["127.0.0.1:${OPENCLOUD_DEMO_PORT:-9200}:9200"]`, nicht ungebunden; (2) `scripts/seed-opencloud-demo.py::seed()` ruft `validate_demo_base_url()` als allerersten Schritt auf, vor `discover_demo_photos`, Client-Erzeugung und jedem Netzwerk-Request (testseitig abgesichert durch `test_rejects_non_demo_url_before_any_network_call`); (3) `.gitignore` hat die `!.env.demo.example`-Ausnahme, `.env.demo.example` enthält nachweislich nur Demo-/Platzhalterwerte (u.a. `SECRET_KEY`/`POSTGRES_PASSWORD`/`AUTH_SEED_USER*_PASSWORD` sind erkennbare Demo-Strings, kein reales Secret). Zusätzlich geprüft und unbedenklich befunden: die gegenüber Spec/ADR 0009 zusätzlichen `opencloud-demo`-Env-Vars `OC_URL`, `PROXY_TLS=false`, `OC_INSECURE=true`, `OC_EXCLUDE_RUN_SERVICES=idp` (empirisch beim manuellen Smoke-Test ermittelt, siehe ADR 0010 bzw. Kommentare in `docker-compose.demo.yml`) ändern nichts an der Risikoeinstufung — fest codierte Werte für einen rein lokalen, auf `127.0.0.1` gebundenen, isolierten Demo-Container ohne Bezug zu echten Zugangsdaten; interne Service-Secrets (JWT-/Transfer-Secrets etc.) werden vom Container selbst über `opencloud init` erzeugt und in einem eigenen, nicht versionierten Docker-Volume (`opencloud_demo_config`) persistiert, nicht als Klartext-Env-Var übergeben. Der neue `seed`-Compose-Service (kein Port-Publishing, `profiles: ["seed"]`, `build: ./scripts`) erweitert die Angriffsfläche nicht messbar — kein Dauer-Service, kein exponierter Port, läuft nur einmalig im ohnehin schon isolierten Compose-Netzwerk des Demo-Stacks. CI-Erweiterung (`demo-scripts`-Job, erweiterter `docker-compose-check`-Job) baut/testet/lintet nur, startet nie einen echten `opencloud-rolling`-Container und verwendet keine echten Secrets. Details siehe "Docker-Compose-Netzwerk" und "Bekannte Lücken" unten. Zuvor am selben Tag (Sicherheits-Konsultation zu Spec "Explizites IPv4-only Docker-Netzwerk" (0010), im Zuge des idea-sharpener-Ablaufs: `enable_ipv6: false` im Compose-`default`-Netzwerk ist primär durch ein Dockhand-Deploy-Problem motiviert, nicht durch einen Sicherheitsfund, und tendenziell eher eine Reduktion der Angriffsfläche als eine Vergrößerung — ob es tatsächlich einen zusätzlichen IPv6-Host-Listener für `BACKEND_PORT`/`FRONTEND_PORT` verhindert, ist aber **nicht belastbar zugesichert** (Netzwerk-`enable_ipv6` und Host-Port-Publishing sind unabhängige, versions-/konfigurationsabhängige Docker-Mechanismen), siehe Korrektur in "Angriffsflächen" unten. Bestehende IPv4-Port-Bindung bleibt in jedem Fall unverändert. Keine Auswirkung auf Auth oder interne Container-Kommunikation. Zuvor am selben Tag (Sicherheits-Konsultation zu Spec "Lokal ausprobieren ohne echten OpenCloud-Server" (0009), im Zuge des idea-sharpener-Ablaufs, ADR [`decisions/0009-local-opencloud-demo-stack.md`](../decisions/0009-local-opencloud-demo-stack.md) bereits Accepted: neue, aber bewusst isolierte optionale Angriffsfläche — echter OpenCloud-Single-Container (`opencloudeu/opencloud-rolling`) als `docker-compose.demo.yml`-Overlay mit absichtlich geschwächter Absicherung (`PROXY_ENABLE_BASIC_AUTH=true`, `OC_INSECURE=true`) und öffentlich dokumentierten Demo-Zugangsdaten (Nutzer alan/mary/margaret/dennis/lynn, Passwort `demo`). Bestätigt: kein Risiko für den Produktivstack selbst, da `docker-compose.yml` unverändert bleibt und es ein rein additives, separates Overlay ist. Zwei konkrete Risiken benannt (siehe "Docker-Compose-Netzwerk" und "Bekannte Lücken" unten): (1) versehentliche Exposition des Demo-Containers, falls dessen Port wie im Hauptstack ungebunden (effektiv auf allen Interfaces) exponiert wird, statt explizit auf `127.0.0.1` gebunden zu werden — relevant, da PhotoSort laut Auth-Modell oben bewusst aus dem offenen Internet erreichbar ist; (2) `scripts/seed-opencloud-demo.py` könnte bei versehentlicher Nutzung der produktiven `.env` synthetische Fotos in den echten Familien-Space schreiben, falls es `OPENCLOUD_BASE_URL` ungeprüft übernimmt. Zusätzlich als Implementierungs-Hinweis festgehalten: `.gitignore`s pauschale `.env.*`-Sperre würde die geplante Vorlagedatei `.env.demo.example` selbst treffen, sofern keine `!.env.demo.example`-Ausnahme analog zu `.env.example` ergänzt wird. Zuvor am 2026-07-29 (Security-Konsultation im Zuge des Verfeinerns von Spec 0008 "Automatisierte SemVer-Releases bei Merge nach main", ADR [`decisions/0008-automated-semver-releases.md`](../decisions/0008-automated-semver-releases.md), noch nicht implementiert: Vorausschau-Absatz zur "GitHub-Repository-Zugriff"-Sektion ergänzt — neuer automatisierter Akteur (`RELEASE_PLEASE_TOKEN`-PAT mit Self-Merge) berührt die in ADR 0007 dokumentierte Bedingung für `required_approving_review_count: 0`; Bewertung/Härtungsempfehlungen (Action-Pinning auf Commit-SHA, kein `pull_request_target`, Tag-Protection für `v*`, Rotationsprozess, Secret-Scanning-Verifikation) liegen im `## Security`-Abschnitt von Spec 0008, hier nur der projektweite Vermerk. Definitive, verifizierte Aktualisierung dieser Sektion folgt nach Umsetzung im Security-Review, siehe dort. Zuvor am 2026-07-28 (Sicherheitsfokussiertes Review zu Spec "Automatische Auswahl der besten Fotos, Phase A" (0003), Feature-Branch `feature/0003-automatic-best-photo-selection`: alle im Security-Abschnitt der Spec vorab genannten Muss-Kriterien geprüft und bestätigt — `POST /projects/{id}/score` hängt am selben Router-weiten `dependencies=[Depends(get_current_user)]`-Torwächter wie `scan`, `PhotoScore`/`suggestion` sind projektweit statt nutzerspezifisch sichtbar (konsistent mit dem "kein Innentäter-Modell"-Grundsatz), der Scoring-Job schreibt nachweislich nie eine `Rating`-Zeile (`user_id` bei `PUT /photos/{id}/rating` weiterhin ausschließlich aus dem JWT), die neue Bildverarbeitung in `scoring.py`/`worker.py::_compute_photo_metrics` ist wie in `thumbnails.py` mit breitem `except Exception` pro Foto abgesichert (testseitig belegt, siehe `backend/tests/test_worker_score_project.py`), keine neue Abhängigkeit (`backend/pyproject.toml` unverändert), `duplicate_of` ist rein serverseitig berechnet. Dabei das Best-effort-Bildverarbeitungsmuster als projektweiten Grundsatz unter "Angriffsflächen" verankert (siehe dort), da es nun zum zweiten Mal (nach `thumbnails.py`) unabhängig auftrat. Keine neuen Lücken gefunden. Zuvor am selben Tag: Umsetzung von Spec "GitHub-Repo-Zugriffshärtung & Issue-Freigabe-Vorsorge" (0007), `feature/0007-github-repo-access-hardening`: neue Sektion "GitHub-Repository-Zugriff" mit verifizierter, datierter Baseline für Branch Protection auf `main`, Collaborators, Deploy Keys und GitHub Apps — siehe dort. Zuvor am 2026-07-22 (Sicherheitsfokussiertes Review zu Spec "Minimales Projekt-Frontend" (0005), Feature-Branch `feature/0005-minimal-project-frontend`: beide seit 2026-07-21 offenen Lücken geprüft und als umgesetzt bestätigt — `CORSMiddleware` in `main.py` mit expliziter Origin-Allowlist (`settings.cors_allowed_origins_list()`, kein Wildcard, kein `allow_origin_regex`, `allow_credentials=False`) sowie der `..`-Segment-Filter in `opencloud/client.py::_join`, der nachweislich alle Call-Sites mit nutzergesteuertem Pfad abdeckt (`list_folder`, `get_range`, transitiv `walk`, sowohl über `GET /opencloud/browse` als auch über `POST /projects`). Details siehe "Angriffsflächen" und "Bekannte Lücken" unten. Zuvor am 2026-07-21: Spec "Auth-Implementierung" (0006) umgesetzt und security-reviewt, siehe Feature-Branch `feature/0006-auth`: Auth/JWT, Secret-Key-Startup-Guard und Rate-Limiting als "Bekannte Lücken" auf umgesetzt aktualisiert; neue Lücke zur Reverse-Proxy-Abhängigkeit der Rate-Limiting-IP-Ermittlung sowie offene Rückfrage zu einem Startup-Guard für Seed-Platzhalter-Zugangsdaten ergänzt. Zuvor am 2026-07-20: initial angelegt im Zuge der Sicherheits-Konsultation zu Spec "Minimales Projekt-Frontend" (`features/0005-minimal-project-frontend.md`); noch am selben Tag ergänzt um die konkreten Auth-Entscheidungen aus der Sicherheits-Konsultation zu Spec "Auth-Implementierung" (`features/0006-auth.md`, ADR [`decisions/0005-auth-implementation.md`](../decisions/0005-auth-implementation.md)) — erste Spec mit echter Login-Grenze, JWT-Signing-Secret und Passwort-Handling; noch am selben Tag ein drittes Mal aktualisiert, nachdem Daniel die zuvor offene Rückfrage beantwortet hat: PhotoSort ist tatsächlich aus dem offenen Internet erreichbar, nicht nur LAN/VPN — Rate-Limiting am Login-Endpunkt wechselt damit vom akzeptierten Restrisiko zum Muss-Kriterium für Spec 0006)

## Zweck

Projektweite, von der jeweiligen Feature-Implementierung unabhängige Sicherheitsstrategie. Ergänzt (nicht ersetzt) den `## Security`-Abschnitt der einzelnen Feature-Specs — dort steht, was *pro Feature* an Bedrohungen/Gegenmaßnahmen relevant ist, hier steht, was *projektweit* konsistent gelten soll (Bedrohungsmodell, Auth-Grundsatz, Secrets-Handling, bekannte übergreifende Lücken). Gepflegt vom `security-engineer`-Agenten, siehe `specs/README.md`.

## Bedrohungsmodell

**Schützenswerte Assets:**

- Familienfotos auf OpenCloud (die eigentlich sensiblen Inhalte des Projekts — private Aufnahmen, potenziell auch von Kindern).
- Zugangsdaten zur OpenCloud-Verbindung (`OPENCLOUD_USERNAME`/`OPENCLOUD_APP_TOKEN`) sowie `SECRET_KEY` (JWT-Signing) und Datenbank-Zugangsdaten.
- Nutzer-Accounts (Passwort-Hashes, Argon2, sobald Spec 0006 umgesetzt ist).
- Bewertungen (`Rating`) als personenbezogene, pro Nutzer getrennte Daten — Vertraulichkeit hier ist eine Produktanforderung (siehe `decisions/0003-auth-model.md`), keine primär sicherheitsgetriebene.

**Realistische Angreifer** bei einem privaten, selbstgehosteten Zwei-Personen-Projekt:

- Opportunistische Internet-Scanner/Bots, falls das Homeserver-Setup versehentlich Ports offener exponiert als beabsichtigt (Standard-Credential-Versuche, bekannte CVEs in Basis-Images).
- Client-seitige Angriffe über eine kompromittierte Browser-Session (XSS), z.B. durch unerwarteten Inhalt in Datei-/Ordnernamen, die aus OpenCloud angezeigt werden.
- Kein Innentäter-Modell zwischen Daniel und seiner Frau — beide sind die vertrauenswürdige Basis des Systems; die Trennung der Bewertungen ist ein Produkt-, kein Sicherheitsmerkmal.

**Explizit kein Ziel:**

- Schutz vor staatlichen Akteuren oder gezielten, ressourcenstarken Angreifern.
- Schutz vor physischem Zugriff auf den Homeserver selbst.
- Schutz vor den beiden legitimen Nutzern gegeneinander (kein Zero-Trust-Modell innerhalb der Familie).

**Vertrauensgrenzen:**

- Browser (Frontend) gilt als nicht vertrauenswürdig gegenüber Secrets — App-Token/DB-Zugangsdaten dürfen ihn nie erreichen.
- Backend gilt als vertrauenswürdig und ist der einzige Ort, der Secrets hält.
- OpenCloud (extern, aber Daniels eigene Instanz) wird als potenziell fehlerhaft/unerwartet antwortend behandelt, nicht als aktiv böswillig.

## Auth-Modell

Siehe [`decisions/0003-auth-model.md`](../decisions/0003-auth-model.md): getrennte Accounts, Argon2-Passwort-Hashing, Sessions via JWT, kein Self-Signup. Konsistent mit dem Bedrohungsmodell oben (zwei bekannte Nutzer, kein Bedarf an Registrierung/OAuth/Rollenmodell).

**Grundsatz, projektweit verbindlich** (aus dem Blocker-Muster von Spec 0002 übernommen, gilt nicht nur dort): Jeder Endpunkt, der auf Nutzer-, Projekt- oder Bewertungsdaten zugreift, muss die FastAPI-Dependency `get_current_user` real gegen ein gültiges JWT prüfen. Eine Platzhalter-Dependency (z.B. fest verdrahteter Nutzer) darf nie nach `main`. Bei Endpunkten mit Bezug zu einem konkreten Nutzer (z.B. `Rating`) wird die `user_id` ausschließlich aus dem JWT-Claim abgeleitet, nie aus Body/Query-Parametern (Broken Object-Level Authorization vermeiden).

**Konkrete Umsetzung** (entschieden mit [`decisions/0005-auth-implementation.md`](../decisions/0005-auth-implementation.md) im Zuge von Spec 0006, seit `feature/0006-auth` auch im Code umgesetzt und security-reviewt):

- Passwort-Hashing mit `argon2-cffi`, JWT mit `PyJWT`, Algorithmus **HS256**, signiert mit `settings.secret_key`, `exp` = 30 Tage, kein Refresh-Token, kein Logout-Endpunkt (kein Server-Session-Store, der etwas invalidieren könnte — Tokens sind bis Ablauf gültig, auch nach Passwortänderung oder falls ein Account gelöscht würde; akzeptierte Konsequenz der Refresh-losen 30-Tage-Session, siehe ADR 0005).
- Token-Transport: ausschließlich `Authorization: Bearer <token>`-Header, Speicherung im Frontend über `localStorage`. **Damit ist die vormals offene CSRF-Frage entschieden: kein CSRF-Schutzmechanismus nötig** (Browser hängen den Authorization-Header nie automatisch an fremdinitiierte Requests an, anders als bei Cookies) — siehe Frontend-Abschnitt unten für die Kehrseite (erhöhte XSS-Relevanz durch `localStorage`).
- **JWT-Decode muss den erlaubten Algorithmus explizit fixieren** (`jwt.decode(token, settings.secret_key, algorithms=["HS256"])`, nie `algorithms` weglassen oder aus dem Token-Header übernehmen) — sonst klassische Algorithm-Confusion-/`alg: none`-Schwachstelle, die eine Signaturprüfung komplett umgehen könnte. Muss-Kriterium für Spec 0006.
- **`settings.secret_key` muss beim Start hart geprüft werden**, nicht erst beim ersten Signiervorgang: Anwendung darf nicht starten, wenn `secret_key` dem dokumentierten Platzhalter `"change-me"` entspricht oder kürzer als 32 Zeichen ist. Begründung: Der Platzhalter steht bereits öffentlich in `.env.example` und in dieser Spec/ADR — ein HS256-Secret, das diesem bekannten Wert entspricht, erlaubt beliebige JWT-Fälschung und damit vollen Zugriff auf beide Accounts (und darüber transitiv auf alle Familienfotos, da das Backend als OpenCloud-Proxy fungiert). Muss-Kriterium für Spec 0006, siehe dort.
- **Anti-Enumeration bei `POST /auth/login`**: identische 401-Antwort (Status, Body, Header) bei unbekanntem Username und bei falschem Passwort; Verifikation läuft in beiden Fällen gegen einen echten Argon2-Hash (bei unbekanntem Username gegen einen fest hinterlegten Dummy-Hash mit identischen Kostenparametern wie reale Hashes), damit die Laufzeit nicht unterscheidbar ist. Verbleibende Mikro-Differenzen (ein zusätzlicher, aber ebenfalls indizierter DB-Lookup) sind für dieses private Zwei-Nutzer-Setup als nicht praktisch ausnutzbar eingestuft — kein zusätzlicher Härtungsaufwand (z.B. künstliche Verzögerung) nötig.
- **Eingabegrößen am Login-Endpunkt begrenzen** (`username`/`password` via Pydantic `max_length`, z.B. 128 Zeichen) — verhindert, dass ein übergroßer Passwort-Body unnötig CPU-Zeit im (bewusst langsamen) Argon2-Verify bindet; einziger unauthentifizierter Endpunkt mit echter Rechenlast.
- **Rate-Limiting an `POST /auth/login`, IP-basiert, seit Klärung der Internet-Erreichbarkeit ein Muss-Kriterium/Blocker für Spec 0006** (nicht mehr Soll, siehe "Bewusst akzeptierte Restrisiken" für die Historie dieser Einschätzung): Daniel hat bestätigt, dass PhotoSort tatsächlich aus dem offenen Internet erreichbar ist (auch seine Frau soll unterwegs zugreifen können), nicht nur aus LAN/VPN. Damit ist der Login-Endpunkt einem unbegrenzten Kreis potenzieller Angreifer ausgesetzt, und Argon2 allein (~50-200ms/Versuch) bremst verteilte automatisierte Credential-Stuffing-/Brute-Force-Versuche nicht ausreichend. Konkrete Umsetzung: `slowapi` (FastAPI-Middleware auf Basis von `limits`), Limit **5 Versuche pro Minute pro Client-IP** an `POST /auth/login` (kein globales API-weites Limit nötig, nur dieser eine unauthentifizierte Endpunkt mit Rechenlast), Zähler-Speicherort **Redis** über die bereits vorhandene `settings.redis_url` (kein neuer Infrastruktur-Baustein — Redis ist über `arq`/`REDIS_URL` ohnehin schon Teil des Compose-Stacks, `docker-compose.yml`), damit das Limit über mehrere Backend-Worker/Prozesse hinweg konsistent gilt statt nur in-memory pro Prozess. Bei Überschreitung: `429 Too Many Requests`, keine Detailauskunft über verbleibende Sperrzeit im Body nötig. `slowapi` ist eine neue Backend-Abhängigkeit (`backend/pyproject.toml`) — der `architect` sollte prüfen, ob dafür ergänzend zu ADR 0005 ein kurzer Vermerk sinnvoll ist, da CLAUDE.md neue externe Abhängigkeiten grundsätzlich als architekturrelevant einstuft.

Wie das JWT übertragen wird ist damit entschieden (siehe oben), ebenso das Rate-Limiting (siehe Bullet oben) — beide vormals offenen Punkte aus dieser Spec sind damit geklärt.

## Secrets-Handling

- App-Token/Zugangsdaten ausschließlich über Umgebungsvariablen (`.env`, nie eingecheckt), `.env.example` dient als Vorlage ohne echte Werte (aktueller Stand geprüft — enthält nur leere Platzhalter für `OPENCLOUD_*`/`ANTHROPIC_API_KEY`).
- `docker-compose.yml` reicht Secrets als Umgebungsvariablen an die Container weiter, keine Secrets in der Compose-Datei selbst. Die dortigen Fallback-Defaults (`change-me`, `photosort`) sind bewusste Platzhalter, keine scharfen Secrets im Repo — siehe Restrisiken.
- Keine Secrets in Code, Specs, Logs oder Fehlermeldungen. Aktuell geprüft: `OpenCloudError`-Meldungen (`opencloud/client.py::_raise_for_status`) geben nur Statuscode/Reason-Phrase aus, keine Auth-Header/Tokens — dieses Muster ist beizubehalten, wenn neue Fehlerpfade entstehen.
- **Seed-Migration (Spec 0006, `AUTH_SEED_USER1/2_USERNAME/PASSWORD` aus `.env`)**: Passwörter dürfen nirgends geloggt werden — kein `print()`/Debug-Log mit Klartext-Passwort, keine Fehlermeldung, die das Passwort einbettet. Geprüft: `backend/alembic.ini` setzt `logger_sqlalchemy` auf `WARNING` (keine SQL-Statement-/Parameter-Logs) und `logger_alembic` auf `INFO` (nur Revisions-Fortschritt, keine Daten) — der Default ist sicher, solange niemand diese Logger versehentlich auf `DEBUG` stellt oder `echo=True` auf der Engine setzt (aktuell nicht der Fall, `db.py::make_engine`). Als Test-Kriterium für Spec 0006 festzuhalten: Migrations-Stdout/-Stderr in einem Test abfangen und sicherstellen, dass die konfigurierten Passwörter dort nie auftauchen.
- App-Token/DB-Zugangsdaten sind nie clientseitig sichtbar: Das Frontend hat keinen direkten OpenCloud-Zugriff, sondern kommuniziert ausschließlich über die Backend-API (siehe `architecture/0001-overview.md`) — diese Architekturentscheidung ist zugleich die wichtigste Absicherung gegen Token-Exposition im Browser und wird hier bewusst als Sicherheitsgrundsatz festgehalten, nicht nur als Architekturdetail.

## GitHub-Repository-Zugriff

**Baseline verifiziert am 2026-07-28** (Umsetzung von Spec [`0007`](../features/0007-github-repo-access-hardening.md), ADR [`decisions/0007-github-repo-access-hardening.md`](../decisions/0007-github-repo-access-hardening.md)) — manuell zu pflegende Momentaufnahme, kein automatisiertes Drift-Scanning (siehe "Bewusst akzeptierte Restrisiken" unten). Vor Anbindung des externen Deploy-Tools "Dockhand" erneut zu prüfen, sobald dessen konkretes Zugriffsmodell feststeht.

- **Branch Protection auf `main`** (`gh api repos/TheRealKoller/photosort/branches/main/protection`), per Objekt-Replace (`PUT`) auf den in ADR 0007 festgelegten Ziel-Zustand gebracht, vollständiger Vorher/Nachher-Diff über alle Felder gezogen (keine unbeabsichtigten Nebenwirkungen):
  - `required_status_checks`: `strict: true`, Checks `backend`, `frontend`, `docker-compose-check` — unverändert.
  - `enforce_admins: true` (vorher `false`) — gilt jetzt auch für Daniels Admin-Account, kein Bypass mehr für rote CI-Checks.
  - `required_pull_request_reviews.required_approving_review_count: 0` (vorher `1`, faktisch nie erfüllbar). **Empirisch verifiziert:** Die GitHub-API akzeptiert `0` trotz einer Doku, die einen Wertebereich 1–6 nennt — das gesamte `required_pull_request_reviews`-Objekt musste entgegen dem in der Spec vorgesehenen Fallback nicht entfernt werden. `require_code_owner_reviews: false` bleibt explizit gesetzt (kein CODEOWNERS bei einem Collaborator).
  - `required_conversation_resolution: true` (neu, vorher nicht gesetzt).
  - `allow_force_pushes: false`, `allow_deletions: false`, `restrictions`: nicht gesetzt (auf diesem GitHub-Free-Personal-Plan für ein Nicht-Org-Repo generell nicht verfügbar — der `.../protection/restrictions`-Subendpunkt liefert `404 "Push restrictions not enabled"`, weder vor noch nach der Änderung — kein Push-Restriction-Feature nötig bei genau einem Collaborator).
  - **Funktionaler Nachweis** (Wegwerf-PR #4, `chore/0007-branch-protection-repro`, geschlossen ohne Merge): GitHubs eigenes berechnetes `mergeable_state`-Feld bestätigte den Effekt direkt — `blocked` bei rotem `backend`-Check (Beleg für `enforce_admins: true`, auch für den Admin-Account), weiterhin `blocked` bei grüner CI aber unaufgelöstem Review-Thread (Beleg für `required_conversation_resolution: true`), und `clean` unmittelbar nach Auflösen des Threads. Ein tatsächlicher Merge-Versuch über `gh pr merge`/die Merge-API wurde von der lokalen Agenten-Sandbox (Auto-Mode-Classifier, nicht GitHub) unterbunden — `mergeable_state` ist GitHubs eigene autoritative Einschätzung und damit ein gleichwertiger Nachweis.
- **Collaborators** (`gh api repos/TheRealKoller/photosort/collaborators?affiliation=all`): genau ein Eintrag, `TheRealKoller`, `permissions.admin: true`. Keine offenen Einladungen (`.../invitations`: leer).
- **Deploy Keys** (`gh api repos/TheRealKoller/photosort/keys`): leeres Array, keine vorhanden.
- **GitHub Apps mit Repo-Zugriff:** Der einzige in Check-Runs sichtbare App-Actor ist `GitHub Actions` (App-ID 15368) — das first-party Actions-System, das die im Repo selbst definierten Workflows (`.github/workflows/ci.yml`) ausführt; erwartet und unbedenklich, kein externer Actor. Der Copilot-Reviewer (`gh pr edit <PR> --add-reviewer "@copilot"`, siehe `CLAUDE.md`) taucht **nicht** in der Collaborators-Liste auf und hat damit keinerlei Push-/Schreibzugriff auf Code — nur die projektweit übliche Kommentar-/Review-Rolle.
- **Einordnung (siehe ADR 0007 für die vollständige Herleitung):** Diese Härtung schützt vor Daniels eigenen versehentlichen Aktionen (Force-Push, Merge bei rotem CI, übersehener offener Kommentar-Thread) und vor einem künftigen, nicht-administrativen Automatisierungs-Token — nicht vor einer vollständigen Kompromittierung des einzigen Admin-Accounts selbst (2FA/Account-Absicherung bleibt außerhalb des Scopes, Daniels persönliche Verantwortung). `required_approving_review_count: 0` ist nur so lange unproblematisch, wie ausschließlich Daniel Schreibzugriff hat — bei einem zweiten Schreibzugriffs-Actor (z.B. Dockhand) ist das erneut zu bewerten.
- **Vorausschau auf Spec/ADR 0008 (automatisierte SemVer-Releases, Security-Konsultation 2026-07-29, zum jetzigen Stand noch nicht implementiert):** Führt einen neuen automatisierten Akteur ein — Repo-Secret `RELEASE_PLEASE_TOKEN`, ein fine-grained PAT unter Daniels eigenem Account, gescoped auf genau dieses Repo (`Contents: Read & write`, `Pull requests: Read & write`, `Metadata: Read`), mit dem ein Workflow (`release-please-action` + `gh pr merge --auto`) einen Release-PR **ohne menschlichen Klick** selbst mergt (GitHub natives Auto-Merge). Formal entsteht kein neuer Collaborator-Eintrag (Token läuft unter Daniels Identität), real aber ein neuer, unbeaufsichtigter automatisierter Merge-Pfad nach `main` — die oben genannte Bedingung für `required_approving_review_count: 0` ("sicher, solange Daniel alleiniger Schreibzugriffs-Inhaber ist") ist damit im Geiste, wenn auch nicht formal, berührt. Von Daniel als bewusste, akzeptierte eigene Automatisierung bestätigt (siehe ADR 0008, Kontext-Abschnitt) — hier nicht erneut infrage gestellt. Vollständige Bedrohungsanalyse und Härtungsempfehlungen (Commit-SHA-Pinning der Action, kein `pull_request_target`-Trigger, Tag-Protection für `v*` bislang fehlend, Rotationsprozess für das PAT, Secret-Scanning-Verifikation) stehen im `## Security`-Abschnitt der Spec 0008. **Bei Umsetzung** ist diese Sektion hier um die dann tatsächlich verifizierten Werte zu ergänzen (analog zur Baseline aus ADR 0007), insbesondere: reales PAT-Ablaufdatum, ob Tag-Protection ergänzt wurde, `default_workflow_permissions` des Repos.

## Angriffsflächen

### REST-API (Backend)

- Aktuell (Stand Spec 0001) implementierte Endpunkte (`/opencloud/browse`, `/projects` CRUD) sind noch **nicht** durch Auth geschützt, da `get_current_user` noch nicht existiert — siehe Bekannte Lücken.
- Input-Validierung über Pydantic-Schemas an der API-Grenze ist Pflicht für jeden Endpunkt. Query-/Body-Parameter, die in Pfad- oder Dateioperationen einfließen (z.B. `path` bei `/opencloud/browse`, `variant` bei künftigen Bild-Endpunkten), müssen vor Weitergabe an den OpenCloud-Client bzw. an Dateisystemzugriffe serverseitig eingeschränkt/normalisiert werden (siehe WebDAV-Client unten und das Path-Traversal-Muster in Spec 0002 zum `variant`-Parameter).
- **CORS — umgesetzt mit Spec 0005** (`feature/0005-minimal-project-frontend`, security-reviewt 2026-07-22): `backend/src/photosort/main.py` konfiguriert `CORSMiddleware` mit `allow_origins=settings.cors_allowed_origins_list()` (neue Setting `cors_allowed_origins`, Default `"http://localhost:5173,http://localhost:8080"`, komma-getrennt, über `CORS_ALLOWED_ORIGINS` konfigurierbar), `allow_credentials=False`. Geprüft: keine `allow_origin_regex`-Nutzung, kein Wildcard `*`, keine Reflektion beliebiger Origins — `backend/tests/test_cors.py` deckt sowohl den Erlaubt- als auch den Verboten-Fall sowie Preflight (`OPTIONS`) explizit ab. `allow_credentials=False` ist konsistent mit ADR 0005 (Bearer-Header statt Cookie).
- **`POST /auth/login`** (Spec 0006) ist der einzige beschriebene, bewusst unauthentifizierte Endpunkt mit echter Rechenlast (Argon2-Verify) und echtem Nutzereingabe-Angriffsvektor (Credential-Stuffing/Brute-Force). Siehe Auth-Modell oben für Anti-Enumeration, Eingabegrößen-Limit und Algorithmus-Pinning beim JWT-Decode. `get_current_user` (`HTTPBearer(auto_error=False)`) muss bei fehlendem/ungültigem/abgelaufenem Token oder nicht mehr existierendem User einheitlich 401 liefern, nie stillschweigend durchlassen.

### Lokale Bildverarbeitung (projektweites Robustheitsprinzip)

**Grundsatz, projektweit verbindlich** (erstmals als konkreter Fund in `thumbnails.py::generate_variants` zu Spec 0002 aufgetreten, seit Spec 0003 (`scoring.py`/`worker.py::_compute_photo_metrics`) ein zweites Mal angewendet und hier deshalb als wiederkehrendes Muster verankert statt nur im jeweiligen Code-Kommentar zu leben): Jede Einzelfoto-Verarbeitung auf potenziell außergewöhnlichen, aber nicht böswilligen Bilddaten (ungewöhnliche Auflösung/Seitenverhältnis, beschädigte/nicht dekodierbare Datei, Randfälle wie eine vollständig einfarbige Fläche) läuft in einem pro-Foto isolierten Best-effort-Block mit bewusst breitem `except Exception` — eine engere Exception-Liste ist unzureichend, da z.B. `PIL.Image.DecompressionBombError` nicht von `OSError` erbt und durchgelassen würde. Ein einzelnes problematisches Foto darf nie einen gesamten Job (`ScanRun`/`ScoringRun`) abbrechen oder dauerhaft auf `running` hängen lassen. Gilt für jeden künftigen Job, der Bilddaten lokal verarbeitet (Referenzimplementierungen: `thumbnails.py::generate_variants`, `worker.py::_compute_photo_metrics`).

- Bei Spec 0003 zusätzlich entschärft dadurch, dass die Heuristik-Berechnung (Schärfe/Belichtung/dHash) auf der bereits von `thumbnails.py` auf max. 2048×2048 Pixel begrenzten `display`-Cache-Variante läuft, nicht auf dem OpenCloud-Originalbild — `DecompressionBombError` beim erneuten Öffnen ist damit praktisch ausgeschlossen, das breite `except Exception` bleibt trotzdem Pflicht für die übrigen Randfälle (fehlende Cache-Datei, beschädigte Datei, Varianz-0-Bilder o.ä.).

### WebDAV-/OpenCloud-Client

- Externes System; Umgang mit malformed XML/unerwarteten Statuscodes ist bereits über `webdav_xml.py`/`OpenCloudError` abgedeckt (Stand Spec 0001).
- **Path-Traversal — umgesetzt mit Spec 0005** (`feature/0005-minimal-project-frontend`, security-reviewt 2026-07-22): `_join()` in `backend/src/photosort/opencloud/client.py` zerlegt den Pfad weiterhin in `/`-getrennte Segmente, lehnt aber jetzt vor dem URL-Encoding jedes Segment `== ".."` ab (wirft `OpenCloudError`, kein Request geht raus). Geprüft, dass der Fix wirklich alle Call-Sites mit nutzergesteuertem Pfad abdeckt: `list_folder` und `get_range` rufen `_join` direkt auf, `walk` transitiv über `list_folder`; beide Nutzereingabe-Pfade (`GET /opencloud/browse?path=...` sowie `opencloud_path` bei `POST /projects`, das zusätzlich per `list_folder`-Existenzcheck validiert wird, bevor es persistiert wird) laufen nachweislich durch `_join`. Umgehungsversuche geprüft und für nicht wirksam befunden: URL-kodierte `..` werden von FastAPI bereits vor `_join` einmalig dekodiert (Query-Parameter) bzw. kommen als reiner JSON-String an (Body) — es gibt keinen zweiten, unkontrollierten Dekodierschritt zwischen Nutzereingabe und dem Segment-Vergleich; doppelte Slashes erzeugen leere, herausgefilterte Segmente und verstecken kein `..`; Windows-Backslashes sind für HTTP-/WebDAV-URLs kein Pfadtrenner und daher irrelevant; NULL-Bytes/Unicode-Dot-Lookalikes würden zwar den exakten String-Vergleich unterlaufen, ergeben aber keine gültige `..`-Sequenz beim Zielserver (rein theoretisches Restrisiko, für dieses private Setup ohne aktiv böswillige OpenCloud-Instanz als nicht praktisch relevant eingestuft). Tests in `backend/tests/test_opencloud_client.py` decken `_join` direkt sowie `list_folder`/`get_range` mit ablehnendem Pfad ab (Mock-Transport wirft `AssertionError`, falls doch ein Request rausginge).
- SSRF-Betrachtung: `base_url`/`webdav_url` stammen ausschließlich aus serverseitiger Konfiguration (`.env` bzw. dem in der DB gespeicherten `opencloud_drive_id`/`opencloud_path` eines Projekts), nie aus direkter Nutzereingabe des Ziel-Hosts — kein SSRF-Risiko über die Wahl des Ziels, nur über den Pfad-Anteil (siehe oben).

### Frontend

- Kein direkter OpenCloud-Zugriff vom Client — Secrets bleiben ausschließlich serverseitig (siehe Secrets-Handling).
- XSS: Datei-/Ordnernamen von OpenCloud sind letztlich außerhalb der Anwendung entstandener Text und werden im Ordner-Browser (Spec 0005) sowie später in Grid-/Vergleichsansicht (Spec 0002) angezeigt. React escaped Textinhalte standardmäßig — Grundsatz: kein `dangerouslySetInnerHTML` für solche Werte, auch nicht "weil es ja nur die eigene OpenCloud-Instanz ist". **Gilt unverändert für die mit Spec 0012 eingeführten Tailwind-/Radix-/shadcn-ui-Komponenten** (siehe Konsultation im Kopf dieses Dokuments): Datei-/Ordnernamen dürfen auch in neuen Tooltip-/Dialog-/Popover-artigen Komponenten nur als React-Kinder (Text), nie als HTML-String-Prop übergeben werden. **Seit ADR 0005 mit erhöhtem Einsatz:** Das Session-Token liegt in `localStorage` (nicht httpOnly-Cookie) und ist damit für jedes erfolgreich eingeschleuste Skript unmittelbar auslesbar (Token-Diebstahl, effektiv Session-Hijacking für bis zu 30 Tage ohne Widerrufsmöglichkeit). Die Stakeholder-Entscheidung dafür (siehe ADR 0005) stützt sich explizit darauf, dass es *aktuell* keinen nutzergenerierten HTML-Inhalt und keine Drittanbieter-Skripte gibt — das `dangerouslySetInnerHTML`-Verbot ist damit von einer allgemeinen Konvention zur tragenden Voraussetzung dieser Auth-Entscheidung geworden. Jedes künftige Feature, das einen der beiden Faktoren ändert (z.B. Freitext-Notizen zu Fotos, Einbindung eines externen Skripts/Trackers), macht diese Restrisiko-Abwägung hinfällig und muss hier erneut mit dem Stakeholder bewertet werden, nicht nur technisch nachgezogen werden.
- CSRF: **entschieden** (ADR 0005) — Token-Transport ausschließlich via `Authorization`-Bearer-Header, nie als Cookie. Kein CSRF-Schutzmechanismus nötig, da Browser diesen Header nie automatisch an fremdinitiierte Requests anhängen (anders als bei Cookies). Die in Spec 0002/0005 offen gelassene Frage ist damit für beide Specs geschlossen.
- Keine Backend-Secrets im Frontend-Build — falls künftig Frontend-Umgebungsvariablen nötig werden, gilt die Vite-Konvention (`VITE_`-Prefix macht Werte Teil des Client-Bundles), aktuell nicht relevant, da keine vorhanden.

### Docker-Compose-Netzwerk

- Nach außen exponierte Ports: `BACKEND_PORT` (Default 8000) und `FRONTEND_PORT` (Default 8080), beide direkt auf den Host gemappt. `postgres`/`redis` haben bewusst kein `ports:`-Mapping nach außen, nur intern im Compose-Netzwerk erreichbar — korrekt so, beizubehalten.
- Kein TLS/Reverse Proxy in `docker-compose.yml` — siehe Restrisiken unten.
- Solange Auth nicht implementiert ist, ist der Backend-Port für jeden mit Netzwerkzugriff auf den Host praktisch ungeschützt nutzbar (siehe Bekannte Lücken).
- **Optionaler Demo-Stack (Spec 0009, `docker-compose.demo.yml`) — umgesetzt, Security-Review bestanden (`feature/0009-local-opencloud-demo-stack`):** führt einen echten OpenCloud-Container mit bewusst geschwächter Absicherung ein (`PROXY_ENABLE_BASIC_AUTH=true`, `OC_INSECURE=true`, `PROXY_TLS=false`, öffentlich bekannte Demo-Zugangsdaten `demo`). Rein additiv, kein Risiko für `docker-compose.yml`/den Produktivstack selbst. Port nachweislich `ports: ["127.0.0.1:${OPENCLOUD_DEMO_PORT:-9200}:9200"]`, nicht ungebunden — verifiziert. README/`.env.demo.example` weisen zusätzlich explizit auf "nur lokal, nicht auf gemeinsam genutztem Host starten" hin. Zusätzlicher, per Compose-Service (`seed`, `profiles: ["seed"]`, kein Port-Publishing) laufender Seed-Job lädt Beispielfotos hoch; erweitert die Angriffsfläche nicht messbar (kein Dauer-Service, kein Port, gleiches isoliertes Netzwerk wie `opencloud-demo`, siehe ADR 0010). `scripts/seed-opencloud-demo.py::validate_demo_base_url()` wird in `seed()` nachweislich vor jedem Netzwerkzugriff aufgerufen (testseitig abgesichert) — verhindert versehentliches Schreiben in einen echten Familien-Space bei falscher `.env`.
- **IPv4-only Compose-Netzwerk (Spec [`0010`](../features/0010-explicit-ipv4-only-network.md), noch nicht implementiert):** `enable_ipv6: false` im expliziten `default`-Netzwerk nimmt Containern in diesem Netzwerk die IPv6-Adresse — primär motiviert durch das Dockhand-Problem (nicht löschbare IPv6-Netzwerke), nicht durch einen akuten Sicherheitsfund. Ob das auch verhindert, dass Docker für `BACKEND_PORT`/`FRONTEND_PORT` einen zusätzlichen IPv6-Host-Listener anlegt, ist **nicht belastbar zugesichert**: Netzwerk-`enable_ipv6` (Container-seitige Adressvergabe) und Host-Port-Publishing (`ports:`) sind technisch unabhängige Docker-Mechanismen, deren genaues Zusammenspiel von Docker-Version/-Daemon-Konfiguration abhängt. Tendenziell eher eine Reduktion der Angriffsfläche als eine Vergrößerung, aber bei der Umsetzung von Spec 0010 per `ss -tlnp`/`docker port` tatsächlich zu verifizieren statt hier als gesichert zu behaupten. Die bestehende IPv4-Port-Bindung selbst (ungebunden auf allen Interfaces, siehe oben) bleibt in jedem Fall unverändert.

## Bewusst akzeptierte Restrisiken

- **Kein eingebauter Reverse Proxy/TLS** in `docker-compose.yml` — das Homeserver-Setup von Daniel übernimmt TLS-Terminierung (konsistent mit `architecture/0001-overview.md`, Abschnitt "Bewusste Annahmen"). Akzeptiert, da außerhalb des Scopes dieses Repos.
- **Kein Passwort-Reset-Flow per E-Mail** (laut `decisions/0003-auth-model.md`) — vertretbar für zwei bekannte Nutzer im selben Haushalt; Reset kann administrativ (CLI/DB) erfolgen.
- ~~**Kein Rate-Limiting** auf Login-/API-Endpunkten~~ — **nicht mehr als Restrisiko akzeptiert, sondern zum Muss-Kriterium eskaliert.** Ursprünglich war das Fehlen von Rate-Limiting hier als akzeptables Restrisiko eingestuft, ausdrücklich unter der Bedingung "bei zwei bekannten Nutzern und ohne öffentliche Bewerbung des Diensts" bzw. solange der Zugriff auf LAN/VPN beschränkt bleibt. Die dazu offene Rückfrage an Daniel ist beantwortet: PhotoSort ist tatsächlich aus dem offenen Internet erreichbar (auch die Ehefrau soll unterwegs zugreifen können), nicht nur aus dem Heimnetz. Damit entfällt die Grundlage für die Restrisiko-Einstufung — siehe Auth-Modell oben für die konkrete Umsetzung (`slowapi`, 5/Minute pro IP, Redis-Zähler über `redis_url`), jetzt als Muss-Kriterium/Blocker für Spec 0006 geführt, nicht mehr hier unter akzeptierten Restrisiken.
- **Platzhalter-Defaults** in `.env.example`/`docker-compose.yml` (`change-me`, `photosort`) sind bewusst keine scharfen Secrets im Repo — Restrisiko liegt darin, dass Daniel sie lokal tatsächlich ändern muss; kein automatisierter Zwang dazu vorgesehen (kein Blocker bei selbstgehostetem Einzelbetrieb ohne Multi-Tenancy).
- **OpenCloud wird nicht als böswillig behandelt**, nur als potenziell fehlerhaft antwortend — vertretbar, da es sich um Daniels eigene, selbstverwaltete Instanz handelt, kein Drittanbieter-SaaS mit fremdem Zugriff auf die Daten.
- **`GET /projects/{id}/photos` (Spec 0002) liefert immer die Bewertungen beider Nutzer, nicht nur die eigene** — Security-Review-Fund zu `feature/0002-manual-categorization`: Spec 0002 (Abschnitt Security) sieht vor, dass die Grid-/Filter-Ansicht "standardmäßig nur die eigene Bewertung" zurückgeben soll (Datenminimierung), explizit "keine Autorisierungsgrenze". Die Implementierung nutzt denselben Listing-Endpunkt für Grid-, Einzelbild- und Vergleichsansicht (ein Query-Key, eine Datenquelle, `frontend/src/hooks/usePhotos.ts`) und filtert die Anzeige der fremden Bewertung rein clientseitig (`PhotoGridPage.tsx::ownStatus`) statt serverseitig zu trennen. Bewusst akzeptiert statt behoben: beide Nutzer sind laut Bedrohungsmodell oben ohnehin gegenseitig vertrauenswürdig ("kein Innentäter-Modell zwischen Daniel und seiner Frau"), die Trennung ist ein Produkt-, kein Sicherheitsmerkmal (siehe Auth-Modell oben) — eine serverseitige Trennung hätte die einheitliche, elegante Endpunkt-Wiederverwendung zwischen den drei Ansichten unnötig verkompliziert. Kein Blocker, kein Datenleck gegenüber einem unauthentifizierten Dritten (Endpunkt bleibt auth-pflichtig).
- **`GET /photos/{id}/image` und `GET /projects/{id}/photos` prüfen aktuell nicht, ob der angemeldete Nutzer zum jeweiligen Projekt "gehört"** — kein IDOR im klassischen Sinn, da laut Produktkontext (Spec 0002, Abschnitt Security) beide Nutzer ohnehin alle Projekte sehen dürfen; relevant bleibt nur ein gültiges JWT. Konsistent mit dem bereits für Spec 0002 dokumentierten Grundsatz, hier der Vollständigkeit halber referenziert.
- **Kein automatisiertes Drift-Scanning der GitHub-Repository-Zugriffs-Baseline** (Branch Protection, Collaborators, Deploy Keys — siehe "GitHub-Repository-Zugriff" oben) — laut Spec 0007/ADR 0007 explizit Out of Scope, da GitHub-Repo-Einstellungen anders als Code nicht durch Reviews/CI laufen und es kein etabliertes Tooling dafür im Projekt gibt. Akzeptiert, solange nur ein manueller, wiederkehrender Prüfpunkt existiert (mindestens vor Anbindung von Dockhand) statt einer laufenden automatisierten Kontrolle.

## Bekannte Lücken (Stand 2026-07-21)

- ~~Auth/JWT (`get_current_user`) ist spezifiziert, aber noch nicht im Code umgesetzt~~ — **umgesetzt mit Spec 0006** (`feature/0006-auth`): `get_current_user` schützt `/opencloud/browse` und `/projects/*` router-weit, `/health`/`/auth/login` bleiben bewusst öffentlich.
- ~~`settings.secret_key`-Startup-Validierung ist noch nicht implementiert~~ — **umgesetzt mit Spec 0006**: `main.py::create_app` verweigert den Start bei Platzhalter oder < 32 Zeichen.
- ~~CORS-Konfiguration existiert noch nicht~~ — **umgesetzt mit Spec 0005** (`feature/0005-minimal-project-frontend`): `CORSMiddleware` mit expliziter Origin-Allowlist, kein Wildcard, `allow_credentials=False`. Siehe "Angriffsflächen" oben für Details des Reviews.
- ~~Path-Traversal-Lücke in `opencloud/client.py::_join`~~ — **umgesetzt mit Spec 0005**: `..`-Segmente werden vor der URL-Konstruktion abgelehnt, Fix greift nachweislich an allen Call-Sites (`list_folder`, `get_range`, transitiv `walk`) und beiden Nutzereingabe-Pfaden (`GET /opencloud/browse`, `POST /projects`). Siehe "Angriffsflächen" oben für Details des Reviews.
- **Kein automatisiertes Dependency-Scanning** (z.B. `pip-audit`/`npm audit`) in CI etabliert — Prüfung neuer Abhängigkeiten erfolgt aktuell nur manuell im Security-Review (siehe Rollenbeschreibung `security-engineer`); relevant für Spec 0006, da `argon2-cffi`/`PyJWT`/`slowapi` neu hinzukamen (Kurzprüfung: alle drei sind etablierte, aktiv gepflegte Pakete ohne bekannte offene kritische CVEs zum Zeitpunkt dieser Aktualisierung — ersetzt keine echte automatisierte Prüfung).
- ~~Rate-Limiting am Login-Endpunkt ist spezifiziert, aber noch nicht implementiert~~ — **umgesetzt mit Spec 0006**: `slowapi`, 5 Versuche/Minute pro Client-IP, Storage über `settings.redis_url` (testseitig auf `memory://` überschreibbar, siehe `architecture/0002-testkonzept.md`).
- **Neu (Review zu Spec 0006): IP-Ermittlung für das Rate-Limiting ist von der noch nicht feststehenden Reverse-Proxy-Topologie abhängig.** `rate_limit.py` nutzt `slowapi.util.get_remote_address`, das ausschließlich `request.client.host` (den direkten TCP-Peer) auswertet, keine `X-Forwarded-For`/`X-Real-IP`-Header — richtig so, um zu verhindern, dass ein Angreifer das Limit durch selbstgesetzte Forwarded-Header umgeht. Terminiert Daniels Homeserver-Reverse-Proxy (siehe `architecture/0001-overview.md`, außerhalb dieses Repos) TLS auf demselben Host und reicht Requests z.B. über `localhost` weiter, sieht das Backend als `client.host` die Proxy-Adresse statt der echten Client-IP — das Limit würde dann faktisch global statt pro Angreifer greifen (kein Sicherheits-Bypass, aber strenger/anders als das spezifizierte "5 pro Client-IP"). Kein Blocker für Spec 0006 (außerhalb des Repo-Scopes), aber zu prüfen, sobald die reale Proxy-Konfiguration feststeht — ggf. `ProxyHeaders`-Middleware mit Trust nur des einen bekannten Proxy-Hops nötig.
- CSRF-Handling ist **entschieden und geschlossen** (ADR 0005: Bearer-Header, kein Cookie, daher kein CSRF-Schutz nötig) — hier der Vollständigkeit halber nicht mehr als offene Lücke geführt.
- Kein Passwort-Reset-Flow: siehe "Bewusst akzeptierte Restrisiken" oben — bewusste Entscheidung, keine Lücke im engeren Sinn, hier der Vollständigkeit halber referenziert.
- **Seed-Platzhalter-Zugangsdaten (`AUTH_SEED_USER1/2_USERNAME/PASSWORD = "change-me"`/`"change-me-2"`) haben keinen Startup-Guard analog zu `secret_key`.** Im Review zu Spec 0006 aufgeworfen und bewusst offen gelassen (Rückfrage an den Stakeholder nötig, keine reine technische Entscheidung): vergisst Daniel, diese Werte in `.env` zu setzen, entsteht ein real einloggbarer Account mit öffentlich bekanntem Usernamen+Passwort an einem aus dem offenen Internet erreichbaren Endpunkt. Rate-Limiting mildert Brute-Force, verhindert aber nicht den einen Versuch mit bekannten Default-Credentials. Optionen: (a) Startup-Guard analog `secret_key` ergänzen, oder (b) wie beim Postgres-Passwort als akzeptierte Betriebsverantwortung ohne Code-Zwang belassen.
- ~~Spec 0009 (lokaler OpenCloud-Demo-Stack) ist noch nicht implementiert~~ — **umgesetzt mit Spec 0009** (`feature/0009-local-opencloud-demo-stack`), alle drei vorab benannten Muss-Kriterien verifiziert: (1) `docker-compose.demo.yml` bindet den Port explizit auf `127.0.0.1`. (2) `scripts/seed-opencloud-demo.py::seed()` validiert `OPENCLOUD_BASE_URL` als ersten Schritt vor jedem Netzwerkzugriff. (3) `.gitignore` hat die `!.env.demo.example`-Ausnahme, Inhalt enthält nachweislich nur Demo-Werte. Siehe "Angriffsflächen" oben für Details des Reviews. Kein neuer Eintrag hier — keine offenen Punkte gefunden.
- **Bekannter, vorbestehender Funktionsbug außerhalb dieses Reviews (nicht primär sicherheitsrelevant):** Beim manuellen Smoke-Test von Spec 0009 wurde festgestellt, dass `backend/src/photosort/opencloud/client.py::list_drives` das von der Graph-API zurückgelieferte `webDavUrl`-Feld vermutlich an der falschen Stelle erwartet (verschachtelt unter `root.webDavUrl`, nicht auf oberster Ebene — siehe `scripts/seed-opencloud-demo.py::_drive_webdav_url`, das dies bereits korrekt eigenständig implementiert). Nicht Teil von Feature-Branch 0009 (Produktivcode explizit unangetastet). Keine unmittelbare Sicherheits-Nebenwirkung erkennbar (kein Auth-Bypass, keine Injection, kein Datenleck) — im schlimmsten Fall ein funktionaler Fehler (falsche/fehlende WebDAV-URL), kein Sicherheitsrisiko. Zur Nachverfolgung hier vermerkt, damit es nicht verloren geht; Fix gehört in einen eigenen, spec-losen Bugfix-PR gegen `backend/src/photosort/opencloud/client.py` mit Test.
